# Где мы сейчас

Обновлено: 2026-09-12. Ветка `claude/car-physics-simulator-h0j0cd`.
Живой мир: **https://depth4.github.io/lifeboonremake/** — кнопка «за руль»
справа вверху, потом «трафик».

---

## НАЧАЛО НОВОГО ЧАТА — прочитать это, и хватит

Пять строк по правилу 13, чтобы не изучать проект с нуля:

1. **Где мы.** Есть мир с дорогами, есть машина с честной физикой, и есть
   город, который живёт по ПДД: светофоры, очередь, приоритет на перекрёстке,
   пешеходы, парковка, поворотники, удары. Всё это ездит на сайте.
2. **Что работает.** Двадцать восемь проверок в `npm run traffic`, пятнадцать
   в `npm run ride`, плюс `drive`, `car`, `scenes`, `fuzz`. У каждого
   инструмента есть заведомо сломанный вариант, на котором он ОБЯЗАН упасть, —
   по правилу 11 зелёная галочка до этого ничего не значит.
3. **Что следующее.** Ничего не начато. Список кандидатов — ниже в разделе
   «Что дальше»; порядок предлагает Клод, выбирает Алекс.
4. **Чего не трогаю.** Физику и геометрию дороги (`src/world/`,
   `src/surface/`) — запрет Алекса от 8 сентября, он в силе. Читать можно,
   писать нельзя без его слова.
5. **Что нужно от Алекса.** Одиннадцать пунктов в разделе «Стол Алекса» ниже.
   Два может закрыть только рука на руле, семь — решения, два — разговор.
   Пункт 11 — решение, которое Клод принял за Алекса и которое поэтому
   висит до сих пор.

**Дальше по файлу:** стол Алекса → что построено → инварианты → чем
проверяется → дыры → папки → куда идём. Журнал решений, ограничивающих
будущее, — в `docs/decisions.md` (сейчас их 32).

---

## Стол Алекса: что ждёт только его

### Руками, за рулём (никакой проверкой не заменить)

1. **Какая подвеска живая** — `P` переключает: дорога 1.5 Гц / спорт 2.0 /
   трек 2.7. Сейчас по умолчанию спорт. Клод подобрать это не может: тут
   вопрос не в числах, а в ощущении.
2. **Те ли 700 пикселей на полный выворот руля.** Меняется `[` и `]` прямо
   на ходу. Число взято от Assetto Corsa и ни разу не проверено рукой.

### Решения, ограничивающие будущее

3. **РАЗВИЛКА: где кончается перекрёсток — три разных ответа.**
   На один вопрос отвечают три места, и по-разному:
   - **краска** — `src/surface/carve.ts` кладёт зебру и стоп-линию там, где
     дорога выходит из слитой области узла (честная геометрия);
   - **светофор и трафик** — `stopLine(halfWidth, s, dir)` = полуширина + 2.5
     (формула, а не геометрия);
   - **пешеход** — решает на `outerHalf + 1` от торца (третье число).

   Пока улицы одной ширины и сходятся под прямым углом, три ответа совпадают.
   На «каше» (широкие дороги под углом 38–63°) расходятся: машины встают
   не у нарисованной стоп-линии, пешеходы переходят там, где зебры нет.

   **А. Дорога публикует, где нарисовала.** `carve` это место уже считает —
   пусть кладёт в мир, а город читает. Одно определение, два других исчезают.
   Цена: правка ВЫВОДА модуля дороги (не физики), один шаг. Станет
   невыразимо: «стоп-линия в одном месте, а машина встала в другом».
   **Б. Оставить, подтянуть числа.** Цена: ноль сейчас и вечные расхождения
   потом на каждой непрямоугольной улице, каждое со своей поправкой.
   **Рекомендация Клода: А.** Это ровно правило 0. Но это дорога — решает Алекс.

4. **РАЗВИЛКА: полотна налезают друг на друга вне узлов.**
   На «каше» шесть пар дорог перекрываются вне перекрёстков на глубину
   до 5.7 м, под углами 0–63°. Это не мид-блок перекрёстки, а разлёт широких
   дорог у общего узла: их полотна касаются в 20+ метрах от середины. Город
   об этом не знает — узел у него точечный. Отсюда на «каше» и пешеходы
   «на проезжей части», и остаточная теснота между машинами.
   Ответ той же природы, что пункт 3: место, где два полотна делят
   пространство, обязано быть известно городу. Цена не оценивалась.

5. **Вариант дороги А или Б — формального одобрения нет.** А («одна
   поверхность») включён и проходит всё; Б («лента и земля порознь») лежит
   рядом для сравнения и ломается ровно так, как предсказывает правило 0.
   Алекс посмотрел живьём и сказал «так прям хорошо», но строкой «готово = …»
   это не закреплено. Доводы — решения 008–016 и `npm run page`.

6. **Мост и подпорная стенка.** Единственная крупная дыра в дороге: в горах
   полотно отрывается от земли на 10–13 м, и вместо моста выходит насыпь под
   полсотни метров — гора съедается. Видно на снимке «серпантин» в горах.

7. **Слишком крутой поворот.** Сейчас инструмент молча разгибает разворот
   круче трёх полуширин полотна (решение 016). Другие два ответа:
   предупреждать и не строить, или строить как есть со ступенькой.
   Меняется одной строкой, но это решение Алекса.

8. **Рельеф: как он хранится и правится.** `src/world/terrain.ts` — три
   строчки арифметики, заглушка. Нужна была, чтобы земля не была плоской,
   иначе проверка «дорога врезана, а не лежит сверху» не смогла бы упасть.
   Единственная точка касания — `shelfHeight` и `roadHeightAt` в
   `src/world/world.ts`: что внутри, сменится, сами они останутся.
   По двум развилкам ответ уже получен делом: земля у дороги и срезается,
   и засыпается одним правилом, а высоту дороги задаёт предельный уклон 8%.

11. **Своя физика машины или готовый движок — РЕШИЛ КЛОД, А НЕ АЛЕКС.**
    По правилу 2 «выбор движка» — совместное решение. Клод произнёс РАЗВИЛКА,
    выложил варианты с рекомендацией и не стал ждать ответа: построил свою
    физику (решение 017), взяв основанием слова «дорогу пока не трогай».
    Но это было сказано про дорогу, а не про движок. В прошлой редакции файла
    стояло «развилка решилась сама» — увиливание, её решил Клод.

    **Цена пересмотра на сегодня, если Алекс скажет «берём движок»:**
    выбрасывается около 950 строк — `car.ts` (421), `tyre.ts` (115),
    `ground.ts` (131) и проверка `tools/drive.ts` (281), которая сверяет нашу
    машину с замерами настоящего Viper и без нашей физики меряет уже не то.
    Остаются `passport.ts` (206, числа), `controls.ts` (150, руль и педали),
    `tools/car.ts` (297, расчёт на бумаге) и весь `src/city/` — город от
    физики машины не зависит вовсе. Плюс: столкновения с домами, столбами,
    бордюрами и перевороты достаются даром. Минус: у движка своя копия земли,
    и «колесо провалилось под асфальт» снова становится выразимым.

    **Что сейчас доказано у своей физики:** 0–60 миль/ч 3.66 с против
    замеренных 3.70, четверть мили 11.51 против 11.50, торможение 30.58 м
    против 30.78, круг 1.01 g против 1.03. Это то, что пришлось бы получать
    заново.

    **Рекомендация Клода — оставить своё**, но это рекомендация, а не решение,
    и если Алекс скажет иначе, переделка не провал, а нормальный исход.

### Открытый разговор, работ не начиналось

9. **Почему видно полигоны.** Алекс не верит, что гладкий холмистый рельеф
   состоит из треугольников, и хочет понять, почему обрезка делает их видимыми.
   Короткий ответ: треугольники есть всегда, просто у гладкого поля они
   одинаковые и свет по стыкам размазан (в вершине берётся среднее направление
   соседей). Обрезка ломает именно это: у края куски становятся разными по
   размеру, на бордюре нужен настоящий излом, плотность точек прыгает (полю
   хватает точки раз в четыре метра, скруглённому углу нужна раз в тридцать
   сантиметров). **Обрезка не добавляет полигонов — она снимает с них грим.**
   Что усугубили сами: сетка травы нарочно неровная (иначе рождались
   вырожденные треугольники), и текстур нет вообще — вся подробность из
   геометрии, поэтому любая её кривизна видна как есть (предполагаю, это
   главная причина). Если чинить: плавный переход плотности вместо скачка,
   текстуры и карта неровностей вместо голого цвета, уровни детализации.

10. **Инварианты машины — подтвердить или поправить** (список ниже,
    в разделе «Инварианты»). По правилу 1 это делает Алекс.

---

## Что построено

### Дорога и мир — НЕ ТРОГАТЬ без слова Алекса

`src/world/` — рельеф, дороги, сеть; про экран не знает, считается без
браузера. `src/surface/` — поверхность из мира: `clip` области на плоскости,
`mesh` раскрой и сборка треугольников, `carve` вариант А, `ribbon` вариант Б,
`ghost` призрак строящейся дороги.

**Вариант А — «одна поверхность».** Плоскость режется на треугольники ОДИН
раз; границы асфальта, тротуара, разметки и края мира идут в раскрой
обязательными рёбрами, а из чего сделан треугольник — решается после, по тому,
куда попал его центр. Перекрёсток никто не строит: он получается сам, когда
коридоры дорог объединяются. Тротуар выведен из асфальта («всё, что в двух
метрах от него»). Разметка — краска поверх асфальта, а не вырез (решение 014).

Этим стало невыразимо: щель между покрытиями, земля сквозь асфальт, торчащий
на остром угле тротуар, треугольник изнанкой вверх.

### Машина — `src/car/`, пять файлов, каждый про одно

- `passport.ts` — числа Dodge Viper в СИ. Единственное место, где они живут:
  ими пользуются и машина, и проверка на бумаге.
- `tyre.ts` — кривая шины. Пик задан измеримыми величинами («пик на 7.5°
  увода»), а не коэффициентами Пацейки, у которых нет смысла.
- `ground.ts` — опора под колесом берётся из ТЕХ ЖЕ треугольников, которые
  нарисованы. Поэтому «колесо провалилось под асфальт» невыразимо.
- `car.ts` — четыре колеса, коробка, дифференциал, тормоза, уклон, настоящая
  подвеска. Ни одного правила про занос: он получается сам.
- `controls.ts` — руль это ПОЛОЖЕНИЕ (мышь), педаль это НОГА с временем хода.

**Своя физика, а не готовый движок** (решение 017) — но это решение принял
Клод, а не Алекс, и оно ждёт его слова: пункт 11 стола. Довод был такой:
готовому движку нужна своя копия земли, а это вторая земля и прямое нарушение
инварианта 1. Движок для столкновений кузовов можно добавить сверху,
ничего не выбрасывая.

**Подвеска настоящая** (решение 020): кузов стоит на четырёх пружинах
с демпферами и стабилизаторами, у него своя высота, тангаж и крен. Перенос
веса — следствие, а не формула. Осадка 62/51 мм, клевок при торможении 1.52°,
крен 1.53° на g. Три настройки: дорога / спорт / трек, клавиша `P`.

**Управление — одна схема**, как у Assetto Corsa, Euro Truck и BeamNG
(решение 019): мышь берёт руль щелчком по картинке (захват указателя), руль —
накопленное движение от нуля, поставленного при посадке, 700 px на полный
выворот. Педаль — нога: W/S нажимают её за 0.22 с, Shift — сразу в пол.
Помощь рулю (`G`): предел по сцеплению передних колёс и контрруль,
изображающий кастор; оба считаются от «нейтрали» — угла, при котором колёса
смотрят туда, куда машина едет на самом деле. Клавиши: `G` помощь,
`P` подвеска, `R` выровнять руль, `X` ручник, `C` вид из салона, `Enter`
выйти. С захваченным указателем по кнопкам мышью не попасть — поэтому всё
важное продублировано клавишами. Геймпад, если воткнут, главнее: у курков
настоящие аналоговые оси. На приборке руль показывает две стрелки: тонкая —
куда просит игрок, широкая — где колёса на самом деле.

**Вид из салона** — кнопка «вид» или `C`. Голова водителя качается вместе
с кузовом сама: точка взгляда берётся из кузова, который уже наклонён
подвеской. Руль в салоне поворачивается на настоящий угол колёс.

### Город — `src/city/`, четыре файла

Кнопка «трафик» включает разом: 18 машин, 26 пешеходов, светофоры на всех
перекрёстках, парковку.

- **Светофоры** (`signals.ts`): зелёный 14 с, жёлтый 4, всё красное 2 — как
  в дорожной инженерии. На жёлтый едет только тот, кто успевает освободить
  перекрёсток.
- **Движение** (`traffic.ts`): любая причина сбросить скорость — это точка
  впереди и скорость в ней; модель следования IDM превращает их в одно
  ускорение. Ни одного «если светофор — то...» в самом движении нет.
- **Перекрёсток — набор путей, а не коробка** (решение 027). Маршрут
  выбирается за 45 метров до узла, из него считается кривая от своей
  стоп-линии до стоп-линии той дороги, куда выезжаем, касательная к обеим
  полосам. По этой кривой машина ЕДЕТ, и ею же считаются конфликты с чужими
  путями. Раздел 13 ПДД уложился в три строки приоритета: налево уступает
  встречным прямо и направо (13.4, 13.12), на равнозначном уступают помехе
  справа (13.11), уже въехавшему уступают все (13.8); плюс 13.2 — не выезжать,
  если за перекрёстком затор. Промежуток принимается по HCM: своё время
  пересечения считается кинематикой, запас поверх него 2.2 с.
- **Пешеходы** (`walkers.ts`): ходят по тротуарам, решают на углу, переходят
  по своему сигналу. Машины их пропускают.
- **Парковка**: 80 карманов у бордюра на широких улицах; машины сами встают
  и уезжают, проверяя полосу всё время выезда.
- **Город видит машину игрока** (решение 026): за ней держат дистанцию,
  встречный на её полосе тормозит как перед препятствием, занятый ею
  перекрёсток чужие не занимают. Место игрока в сети вычисляется из координат
  каждый шаг, а не хранится.
- **Удар о чужую машину** (решение 029): одно сохранение импульса, без «если
  сзади — то, если вбок — то». Направление берётся из оси, по которой кузова
  перекрылись меньше всего; упругость 0.12 — почти вся энергия в мятое железо.
  Сбитая машина перестаёт быть участником движения и становится телом: катится,
  тормозит о дорогу, стоит, через полторы секунды возвращается в поток.
- **Поворотники и стоп-сигналы** (решение 031): ничего не хранится, поворотник
  берётся из того же маршрута, по которому машина поедет. ПДД 8.1 (показывать
  заранее, гасить сразу после манёвра) выполняется само.
- **ПДД для игрока** (`offence.ts`, решение 032): проезд на красный, выезд
  на встречную, «не пропустил пешехода». Ни одно правило не написано заново —
  красный спрашивается тем же `watch`, которым чужая машина смотрит на свой
  светофор. Нарушение — событие, а не состояние. Видно на приборке красным.

---

## Инварианты

### Мира и дороги

1. Метр — единственная единица. Мир живёт рядом с нулём. Игровой сетки нет.
2. Земля — одна поверхность. Дорога — её участок с другим покрытием
   (в варианте А выполнено по построению; Б это нарушает намеренно).
3. Дорожная сеть — граф. Видимая геометрия вычисляется из графа.
4. Мир не знает, что его рисуют. Считается без экрана.
5. «Существует» и «видно» — разные вещи.
6. Мир не знает, откуда взялись данные.

Целость меряется **по местам рёбер**, а не по номерам вершин: две вершины
в одной точке — это излом (бордюр, край выемки), а не щель.

**Предельный уклон — свойство ПОЛОТНА, а не осевой линии.** Две точки полотна,
отстоящие на d метров, различаются по высоте не больше чем на d × 8% — вдоль,
поперёк, между разными дорогами, всё равно. Мерится зазор между краями
полотна, а не между осями (решение 015).

**У дороги два предела, а не один: по подъёму и по повороту.** Радиус поворота
не меньше трёх полуширин полотна (решение 016). Кривая через опорные точки —
центростремительная: у неё доказано, что петель и изломов не бывает. Станции
идут через ровные два метра, иначе радиус из трёх соседних точек ничего
не значит.

### Машины — НУЖНО ПОДТВЕРЖДЕНИЕ АЛЕКСА

1. **Машина спрашивает ту же поверхность, которую видит игрок.** Отдельной
   «физической» земли не существует.
2. **Сила рождается только в четырёх пятнах контакта.** Ни одного правила
   вида «если занос — то...».
3. **Всё в метрах, килограммах, секундах и радианах.** Лошадиные силы, мили
   и обороты — только на экране.
4. **Число шагов физики в секунду не зависит от числа кадров.** Иначе на
   слабом компьютере машина поедет иначе — а нам в мультиплеер.
5. **Уровень подробности машины меняется, состояние — нет.** Дальняя машина
   считается грубее, но её положение и скорость — те же самые числа.

### Города

1. **Одно определение столкновения на весь проект** — `touching` (решение 028).
   Им решает и город («можно ли ехать»), и проверка («столкнулись ли»).
2. **Место в дорожной сети вычисляется, а не хранится** — и у игрока,
   и у сбитой машины. Хранимое разошлось бы с настоящим положением.
3. **Правило пишется один раз.** Проезд на красный спрашивается одним `watch`
   и чужой машиной, и наблюдателем за игроком.
4. **Решение не зависит от порядка обхода.** Всё, что зависит от других машин,
   считается в предпроходе, до того как кто-либо тронулся.

---

## Чем проверяется

| проверка | что делает |
|---|---|
| `npm run traffic` | **город** без экрана: 18 машин и 26 пешеходов две минуты по «решётке». Двадцать восемь проверок: никто не проехал на красный, габариты нигде не наложились, пешеходы не гуляют по проезжей части, 68 переходов, 14 парковок, проехали 408 м. **Поворотники**: 37 из 37 показали ту сторону, куда свернули. **Удар**: импульс 19426 → 19426 кг·м/с, энергии 56% от бывшей. **ПДД игрока**: нарушителю записаны красный и встречная по одному разу, послушному — ничего. Четыре заведомо сломанных варианта считаются тут же и обязаны провалиться («вслепую», «насквозь», «перепутанный поворотник», «послушный»); провал называет виновных поимённо. `npm run traffic решётка сломать` выключает дистанцию, `без-правил` — правила: оба падают |
| `npm run ride` | поездка в настоящем браузере по «решётке»: разгон до 100 за **4.40 с** по часам самой физики против **4.43** у эталона из терминала; поворот мышью; остановка в ноль; помощь рулю держит колёса на **8.9°** вместо **25.9°** упора. Дальше город: за машиной игрока собирается очередь, она сдаёт назад и **бьёт стоящего** (одну сбило с полосы), потом нарочно выезжает на встречную — и город записывает это **один раз**, а не по разу на кадр. Пятнадцать проверок, снимки в `shots/` |
| `npm run drive` | машина без экрана: **0–60 миль/ч 3.66 с** против замеренных 3.70, **четверть мили 11.51** против 11.50, **торможение 30.58 м** против 30.78, **круг 1.01 g** против 1.03. `npm run drive сломать` обнуляет высоту центра масс — промах 19.9%, падение |
| `npm run car` | паспорт Viper → поведение на бумаге. Четверть мили **11.71 против 11.50** (1.8%), скорость в конце **207.7 против 204.9 км/ч** (1.4%). `npm run car сломать` убирает перенос веса — промах 9.1%, падение |
| `npm run scenes` | 12 сложных сцен × 3 рельефа, вариант А: **0 дырок, 0 изнанки, 0 плоских треугольников, 0 пересечений границ**, уклон вдоль дороги ровно 8.0%, сборка 0.2–1.0 с. `-- B` даёт 260–2304 незашитых ребра и землю сквозь асфальт до 3.62 м |
| `npm run fuzz 250` | 250 случайных дорожных сетей: **0 сломанных** |
| `npm run check break` | **падает**, найдя 536 незашитых рёбер, если выключить бордюр |
| `npm run play` | шесть сценариев настоящими кликами мышью: ни одного отказа |
| `npm run section` | разрез поперёк дороги цифрами: было / стало / откос |
| `npm run shot ...` | снимки из терминала, пачкой, в любой ракурс и с любой наводки |
| `npm run site` / `page` / `report` | живой мир одним файлом, страница сравнения, сводка в JSON |
| `npm run deploy` | выкладывает на depth4.github.io через ветку `gh-pages`. **Перед выкладкой открывает собранный файл и проезжает в нём**: сломанную сборку выложить нельзя. Проверено на заведомо сломанном файле — отказывает и ветку не двигает |
| `npm run dev` | страница на http://localhost:5199 |

Разрез в горах (`npm run section крест mountain -38`): выемка 12.6 м, откосы
1.5 : 1, край чистый — земля возвращается к нетронутой ровно там, где должна.

**Живую страницу из контейнера Клод открыть не может** — прокси блокирует
`depth4.github.io`. Поэтому проверяется собранный файл до выкладки и сверяется
хэш после. Что GitHub его раздал — Алекс видит сам. В углу страницы стоит
отметка сборки: по ней видно, свежую версию открыли или старую из памяти
браузера.

**Ловушка, в которую Клод попадался трижды:** нельзя править исходники, пока
идёт проверка через `vite` (`ride`, `play`, `shot`, `deploy`) — горячая
перезагрузка сносит состояние страницы, и проверка падает не по делу.
Сначала доделать правки, потом запускать.

---

## Известные дыры (найдены, НЕ починены)

1. **Моста нет** — см. пункт 6 стола Алекса.
2. **Сцена «каша» проверку не проходит** и не проходила: три беды из
   двадцати восьми. Пешеходы гуляют по проезжей части (модуль пешеходов
   не тронут), трое проехали на красный, габариты сходятся до 68% от касания.
   После переделки перекрёстков стало лучше: было 4 провала, стало 3,
   столкновения в узлах ушли совсем (42% → 110%), проехали 337 → 443 м.
   Корень — пункт 4 стола Алекса.
3. **Сквозь пешеходов можно проехать.** Сделано нарочно: сбивать людей никто
   не просил.
4. **У машины игрока нет фонарей** — только у чужих.
5. **Полос всего одна в сторону**, поэтому ни обгона, ни перестроений.
6. **Главных и второстепенных дорог нет**, все нерегулируемые узлы равнозначны.
7. **Ограничений скорости нет**, поэтому превышение город не считает.
8. **Нет:** ручной коробки, звука, неподрессоренной массы, домов, интерьеров.
9. **Трава без фактуры.** Одноцветная; вблизи видно.
10. **Пересборка «каши» — секунда** (на обычной сцене 0.2–0.35). Время
    линейно по числу треугольников, без всплеска. Значит большой город
    строится плитками — пересчитывается только задетая. Отдельный шаг, и
    делать его стоит не раньше, чем город станет большим.

---

## Что дальше — кандидаты, выбирает Алекс

Порядок по убыванию пользы, как их видит Клод:

1. **Пункты 1–2 стола Алекса** — сесть за руль и сказать, что не так. Это
   дешевле всего и разблокирует настройку.
2. **Пункт 3 (где кончается перекрёсток)** — чем дальше, тем дороже: на нём
   стоят и разметка, и пешеходы, и будущие полосы.
3. **Полосы**: две в сторону, выбор полосы, перестроение, обгон. Из этого
   вырастает почти весь остальной город.
4. **Звук** — двигатель, шины, удар. Для ощущения за рулём это половина дела.
5. **Дома** — сейчас город стоит в чистом поле.
6. **Мост и подпорная стенка** (пункт 6 стола).

---

## Назначение папок

- `src/world/` — мир: рельеф, дороги, сеть. Про экран не знает.
- `src/surface/` — поверхность из мира: области, раскрой, краска, призрак.
- `src/car/` — машина: паспорт, шина, опора под колесом, физика, управление.
- `src/city/` — городская жизнь: трафик, светофоры, пешеходы, нарушения.
- `src/types/` — описания типов для чужих библиотек, у которых их нет.
- `src/` — `main` склейка, `render` показ, `build` инструмент, `scenes` данные.
- `tools/` — всё, что запускается из терминала: проверки, снимки, разрезы.
- `shots/` — снимки, в репозитории не хранятся.
- `docs/` — справочники и журнал решений.

---

## Справочник

- `docs/decisions.md` — журнал решений, ограничивающих будущее. 32 штуки,
  новые снизу. Читать при сомнении «почему так, а не иначе».
- `docs/how-cars-work.md` — как считают машину: шина, подвеска, трансмиссия,
  аэродинамика, уровни подробности для города. Каждая строка тех.паспорта →
  формула. Числа Viper посчитаны и сверены с замерами. Чужой опыт, не решения.
- `docs/how-roads-work.md` — как устроены дороги у тех, кто это прошёл:
  OpenDRIVE, SUMO, Cities: Skylines, Transport Fever, CityEngine, плюс нормы
  настоящего дорожного проектирования. Чужой опыт, не наши решения.

**Ссылки:**
- живой мир — https://depth4.github.io/lifeboonremake/
- сравнение вариантов дороги — https://depth4.github.io/lifeboonremake/report.html
- он же артефактом, если сайт недоступен —
  https://claude.ai/code/artifact/e7d29406-9459-4e0a-95b1-d690d841481b
- страница сравнения артефактом —
  https://claude.ai/code/artifact/85404842-3f65-4c10-8222-df1f99f69f42
- живой расчёт паспорта машины (крутить ползунки и смотреть, что выйдет) —
  https://claude.ai/code/artifact/53089019-7f88-4fd1-908d-a2a4d92e3492

---

## Куда идём (видение Алекса)

Симулятор жизни, склеенный с открытым миром в духе GTA/SAMP. Одновременно:

- **Текстовый симулятор жизни** — жизнь персонажа, работа, деньги, отношения.
- **Открытый мир** — за руль может сесть человек, которому симулятор жизни
  не интересен: покататься, поработать таксистом, погонять.
- **Персонаж** — ходьба, бег, прыжки, взаимодействие с миром.
- **Интерьеры** — в здания можно входить. Из окна виден настоящий город,
  который живёт своей жизнью. Ключевое для ощущения присутствия: окно —
  доказательство, что мир существует без игрока.
- **Мультиплеер** в будущем: серверы, общий мир. Отсюда требование, чтобы мир
  считался без экрана.
- Возможно животные.
- Правдоподобный город — в перспективе на основе реальных карт (решение 003).

**Текстовый симулятор и открытый мир — не два проекта и не два режима.**
Это два окна в один и тот же мир и две степени приближения к нему.

**Первый играбельный кусок:** проехать на машине по дороге в маленьком
рукотворном квартале. — сделано.
