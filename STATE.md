# Где мы сейчас

Обновлено: 2026-09-25. **Правда — в `main`**: с 24.09 её догоняет крючок конца
хода (решение 095). Чат работает в своей `claude/*`, `main` идёт следом сам.
Живой мир: **https://depth4.github.io/lifeboonremake/** — кнопка «за руль»
справа вверху, потом «трафик».

---

## 25.09, ПОЗДНО — ТВЕРДЬ И НОВОЕ НАПРАВЛЕНИЕ

- **Алекс, новое направление:** «город недостаточно реалистичный. Отдай весь
  бюджет симуляции, людям, их жизни, магазинам, инфраструктуре — чтобы
  город жил, реально жил». Это теперь главное; переделка «город из дорог»
  (ниже) — после неё.
- **Алекс о глубине (25.09):** «симуляция насколько возможно реалистичная
  и глубокая: проследить за типом и увидеть, как он заходит в туалет в ТЦ,
  встречает знакомого на улице, флиртует с уличной дамой, садится пьяный
  за руль и разбивается. Потянет ли это наш движок — хороший вопрос насчёт
  three.js». Ответ Клода (файл-обзор отправлен Алексу для разбора с другими
  ИИ): глубина — не вопрос отрисовщика; симуляция уже считается без экрана
  и от three.js не зависит. Нужны уровни детальности (весь город —
  распорядок, рядом и за кем следят — полная), потребности как отклонения
  от плана, знакомые из сида, события с последствиями, интерьеры как графы
  мест. Движок не менять до замера: 200 анимированных людей + интерьер +
  город за окном на машине Алекса. Образцы: Shadows of Doubt, Watch Dogs
  Legion (перепись), The Sims, RimWorld.
- **Алекс о мире (25.09, вечер):** «существует отдельный мир точек — люди
  и их транспорт, — который и есть симуляция, а взгляд игрока рисует то,
  что попадает в кадр. Одни и те же машины с одними номерами паркуются
  у тех же домов, люди работают там же и отдыхают так же — чтобы игрок
  мог запомнить чуваков. И поверх — случайно или специально у соседа
  меняется машина, он переезжает, болеет, умирает». Что уже так
  (проверено): город живёт целиком без оглядки на камеру; житель — из
  номера (квартира, семья, работа, распорядок, одежда, машина и её цвет);
  люди в зданиях не стоят ничего — их место вычисляется из распорядка по
  вопросу. Чего нет: номеров у машин, закреплённого места стоянки, дней
  недели, изменений со временем. Устройство изменений (решено Клодом):
  ОСНОВА + ЖУРНАЛ — основа из номера, как сейчас; журнал — события,
  меняющие основу с какого-то дня: одни вычисляются из календаря (новая
  машина у №812 в такой-то день), другие записываются, когда случились
  (авария, то, что сделал игрок). Город на день N = основа + события до N.
- **ПЛАН «ГОРОД ЖИВЁТ» (объявлен 25.09, уточнён; порядок после слов Алекса
  о мире: сначала лицо машины и её место, календарь, журнал событий
  жизни, потом двери и магазины):** 1) распорядок из
  нескольких дел — СДЕЛАН; 2) улица живёт по распорядку — СДЕЛАН;
  3) следить за жителем — СДЕЛАН;
  4) из подъезда во двор и на тротуар — люди выходят из своих дверей;
  5) магазин живёт: часы работы, покупатели входят и выходят; 6) двор
  живёт — СДЕЛАН; 7) знакомые из сида и встречи на улице — СДЕЛАНО;
  дальше в 7: семьи (дети с родителями), потребности (голод, туалет,
  усталость, общение) как отклонения от плана; 8) автобус; 9) служебные
  машины; 10) дни недели и ночь;
  11) алкоголь и последствия (пьяный за руль — модель водителя хуже).
  Выброшено: «одно дело в сутки», счёт людей по метрам улиц.
- **Горизонт больше не накрывает город** (`render.ts`, `горизонтВокруг`).
  Пешком земля до горизонта рисовалась кольцом от 90 м от середины на
  высоте −0.35 — числами времён, когда мир был квадратом 240 м. В «городе»
  (600 м) кольцо накрывало всю землю ниже −0.35 дальше 90 м от середины:
  41.6% вершин земли «города», 45.0% «большого» — вместе с дорогами
  и машинами (двор у проезда 44: сплошной газон, машины в нём по пояс;
  было и до 25.09 — снято на e57f5f8). Теперь горизонт — квадрат с дырой
  РОВНО ПО ГРАНИЦАМ ЗЕМЛИ, границы берутся из самой поверхности при её
  отдаче показу: накрыть мир ему нечем при любом размере и рельефе.
  Снимок: `shots/свои-места-3.png` — тот же двор: асфальт проезда
  с разметкой, машины на своих местах.
- **У машины своё место у дома и номер** (решение 104). Все 392 кармана
  «города» — на проездах дворов; своё место вычислено раз на город
  (`своиМеста`: пары «хозяин — карман» по расстоянию от подъезда) и от часа
  не зависит: у проезда 44 в 3:00 и в 21:30 те же 17 машин с теми же
  номерами в тех же карманах. У машины на поездку одно поле `стоянка`:
  своё место / любой / нигде (домой без места — во двор, которого нет).
  Госномер «Т359КЕ 73» — из номера хозяина взаимно однозначно (одинаковых
  не бывает), цвет — из той же таблицы, что и имя цвета; панель слежки:
  «машина: бордовая, Т359КЕ 73; у дома своё место». Проверка (`life.ts`,
  17:24–18:24): вернулись домой 150, встали на своё место 122, своё было
  занято гостем у 27, на другое при свободном своём — 1. Показано поломкой:
  «чужое-место» — на своё 10 из 142. Первая редакция без поля «где встать»:
  занято было у 48 из 145 — жильцы без места вставали в чужие свои.
  Номера на самих машинах не нарисованы — только в панели.
- **ПЕРЕДЕЛКА «СЕМЬИ» (объявлена 25.09, 2 шага — оба сделаны; решение 103).** Жители
  бросались по одному — ребёнок мог жить один, дошкольник ходил в сад один.
  **Шаг 1 сделан: квартиры и состав.** Жители дома делятся на квартиры
  (номера подряд; число квартир решает дом, средняя квартира — 2.53,
  ВЫЧИСЛЕНА из переписи 2010, до 25.09 дом делил на 2.4 сам); состав по
  размеру из таблицы `СОСТАВЫ`, первым по типу всегда взрослый — «квартира
  из одних детей» не пишется. Проверка (`life.ts`): квартир в «городе» 2137,
  1…5+: 22.4 / 31.2 / 24.6 / 15.2 / 6.6% при переписи 25.7 / 28.1 / 22.2 /
  15.1 / 8.9 (±4 п.п.); возраст: сад 7.3% (7.5), школа 11.4 (11.5), работа
  54.3 (54.5). Показано поломкой: «все квартиры поровну» — 0 / 46.8 / 53.2 /
  0 / 0; тип ловит `с(Д, Р)`. Подъездов у «города» 305 → 298, у «большого»
  1288 → 1250 (квартир меньше при той же площади). Под теми же номерами —
  другие люди: `?следить=2078` показывает теперь другого человека.
  **Шаг 2 сделан: дошкольника ведут в сад.** Провожающий — пожилой из
  квартиры, если есть, иначе первый работающий (`провожатыйКвартиры`).
  Работающий отводит по пути на работу (машину решает самая дальняя цель
  круга — работа, а не сад) и забирает с работы; пожилой — утром туда
  и обратно, к вечеру за внуком. Дорога с ребёнком — ОДНА запись
  (`Дорога.ведёт`) в дне взрослого и в дне ребёнка. На улице у ребёнка
  нет своего пешехода: он у руки взрослого (`заРуку` в `walkers.ts` —
  одна функция и для показа, и для слежки); в машине провожающего — там
  же, где машина. Панель слежки: «ведёт №…» / «за руку с №…». Проверка
  (`life.ts`, 7:30–8:15): сами по улице шли 0 дошкольников, за руку вели
  208; поездок на жителя 2.75 при 2.57 в обследовании (было 2.58 —
  провожающие добавили дорог; допуск ±20%). В «городе» отводят в сад
  340 взрослых: пешком 279, машиной 61; пожилые 92, работающие 248.
  Ребёнок — с дальней от проезжей части стороны, сторона берётся от улицы,
  а не от взгляда взрослого: 5417 замеров из 5417 (первая редакция —
  «справа по ходу» — ставила к дороге в 2812 из 5417; вторая — «от дороги
  по взгляду» — в 5, когда взрослый оборачивался к догнавшему знакомому).
  Снимок с уровня глаз: `shots/ведут-в-сад-глаз.png` (7:52, взрослый №1672
  с ребёнком №1673 у стены дома, дальше по улице — ещё пара). Снимать
  идущих: крючок `__пешие()` (кто где, кого ведёт) и `__кадр(true)` —
  город живёт с `?кадр=нет` в настоящем времени без картинки, а кадр
  рисуется, когда пара подошла (программный отрисовщик тратит на кадр
  секунды, и дождаться пары с картинкой за полчаса не вышло).
  Показано поломкой: «дети-сами» — сами шли 203. Сверка «на тротуарах по
  распорядку» теперь считает людей, а не пешеходов: ребёнок у руки — тоже
  человек на тротуаре. Во двор дошкольник пока ходит один (двор у дома,
  взрослый — у окна): следующее, если понадобится.
- **Знакомые встречаются на улице** (`city/встречи.ts`, начало шага 7).
  Кто кого знает — ВЫЧИСЛЯЕТСЯ из пары номеров (сосед по дому 0.4 — ВЦИОМ;
  коллега 0.2, одноклассник 0.12, по саду 0.25, приятель по городу 0.004 —
  предположения): в среднем 47 знакомых на жителя (соседей 12, приятелей 22,
  коллег 6, одноклассников 5.5, по саду 2). Идущие навстречу знакомые
  в 1.0–1.6 м друг от друга останавливаются лицом к лицу; говорят по
  очереди (жестикулирует тот, чья очередь); спешащие на работу и учёбу —
  8–30 с, остальные — 40 с–4 мин; пара — раз в сутки. Слежка пишет «встретил
  знакомого — говорит (№…, сосед по дому)» и встречи дня. Проверка
  (`life.ts`, 7:30–8:15): 121 разговор, поговорили 218 человек; стояли от
  1.10 до 1.60 м, сдвинулись 0 см; договорили 202 — так и стоят 0. Вечером
  (17:00–17:45, замер) — 79 разговоров, поговорили 12% прошедших. Показано
  поломкой: «на-ходу» — разошлись на 72 м посреди разговора; «без-конца» —
  стоят 192 из 192; без нижнего края кольца — пара в 8 см друг от друга.
  Цена: ищут встречи только в ряду одного тротуара (улица + сторона) по
  метке вдоль улицы; место в мире — только для кандидатов, основа жителя
  (дом, дело) — раз на жителя (`чем` в `житель.ts` — половина `кто`, без
  распорядка). «Большой город», 8:06, 1247 пешеходов: первая редакция
  (место каждого каждый кадр, полный житель на каждую пару) 2.7 мс на кадр,
  стало 0.46 мс. Снимки: `shots/встреча-сбоку.png` (с уровня глаз, 6 м),
  `shots/встреча-слежка.png` (слежка за №2078 с панелью; сняты до семей —
  с семьями под этими номерами другие люди); адрес с семьями
  `?scene=город&traffic=1&час=18&следить=3819` — через 1.4 с №3819 и №5113
  (коллеги) остановятся поговорить на полторы минуты.
  Общий прогон (`all.mjs`) после шага: всё зелёное, кроме «плавности» —
  один кадр из 177 за машиной с дрожью 7% (95% кадров ≤ 0.1%). Разобрано
  следующим шагом (ниже): причина — в камере, не во встречах.
- **Камера за машиной не дёргается на пропущенном кадре** (`погоня`
  в `render.ts`). «Плавность» падала в 2 прогонах из 7 (и до встреч —
  1 из 4): один кадр с дрожью 4–7%, всегда камера за машиной, сама машина
  0.2%. Причина в устройстве: покадровое сглаживание `lerp(цель,
  1 − e^(−λdt))` верно только для неподвижной цели; у движущейся отставание
  зависит от длины кадра (20 м/с: 2.78 м при 16.7 мс, 2.61 м при 33 мс),
  и пропущенный контейнером кадр давал излом. Теперь — точное решение для
  цели, прошедшей за кадр ровный отрезок: где камера в миг t, не зависит
  от нарезки времени на кадры. Им же ходит камера слежки. Проверка теперь
  пропускает каждый седьмой кадр сама (`?рвано=7`): неровные кадры в каждом
  замере. Было (старое сглаживание, `рвано=7`): 8.0% — падает всегда;
  стало: 0.3–0.4% в трёх прогонах из трёх.
  Замечено, не чинено: дошкольники ходят в сад одни (и встречаются одни) —
  семей и «ведут за руку» нет; мест работы 13 по 75–180 человек (сад на 90
  детей — 125 работников): работа распределена по площади участка.
- **Двор живёт** (`дворНаЧас` в `жизнь.ts`; шаг 6 плана сделан раньше
  4–5 — виднее). Кто по распорядку во дворе — виден: старики на лавках
  у своих подъездов (сидят, по трое; сиденье — по порядку номеров,
  лишние стоят рядом), школьники бегают в коробке, малыши кругами
  у горок и качелей, гуляющие — кругом у площадки. Место ВЫЧИСЛЯЕТСЯ из
  номера и часа, не хранится. Показ оживляет дворы в 180 м от камеры;
  сиденье не зависит от того, где камера (отбор по подъезду с запасом).
  Слежка за тем, кто во дворе, показывает его на его месте. Проверка
  (`life.ts`, 16:30): во дворе по распорядку 507, видно 507 (сидят 286),
  в чужом дворе 0; на лавке теснее всего 3; от сдвига камеры пересели 0.
  Показано поломкой: «все садятся» — на лавке 5; «отбор по кругу камеры» —
  пересели 2 (первая редакция сверки с кругом 40 м этого не ловила).
  Человек умеет сидеть (поза, а не второй человек); партия людей растёт
  запасом, а не пересоздаётся каждый кадр. Рост по возрасту
  (`ростЖителя`: сад 0.62, школа 0.86 взрослого) — у дворовых, пешеходов
  и того, за кем следят, один и тот же; на снимке с уровня глаз
  (`shots/двор-1630-рост.png`) головы школьников стали ниже глаза
  взрослого, до того были выше. Замечено, не чинено: в одну хоккейную
  коробку собирается ~25 школьников сразу — коробка одна на несколько
  домов; разбирать при шаге 4 (выход из своих дверей).
  Пол коробки и площадок уходит на 1 м в землю и стоит на верхе земли
  под следом (`верхЗемли`), а не на отметке центра: рябь пола на
  склоне невыразима.
- **Шаг 3 сделан: следить за жителем.** `?следить=798` или F рядом
  с человеком (пешком или за рулём) — камера идёт за ним сзади-сверху,
  панель слева внизу показывает его день из того же распорядка: кто он,
  где живёт, что сейчас, дороги дня по часам с отметкой текущей. В здании —
  камера ждёт у входа; вышел — снова идёт за ним (ищется по номеру
  жителя, `гдеЖитель` в `жизнь.ts`). Проверка (`life.ts`): у всех 5 410
  есть точка; идущих улицей по распорядку 155, слежка находит на улице 141
  (91%); на подделке `снимок` — 0.
- **На сайте** сборка 25.09 14:23 из baec443 (твердь, распорядок, улица
  по распорядку, стоящие отдельно, слежка). Двор — ещё нет.
- **Стоящие в кармане — не участники движения** (решение 101). После
  шага 2 на «большом городе» в 8:06 на улице стало 1202 машины вместо
  450, и работа кадра БЕЗ отрисовки выросла 16.8 → 30.5 мс (95% кадров
  24.4 → 46.4; контейнер). Профиль: 40% — высота земли под каждой машиной
  каждый кадр, ещё цвета и позы. Стоящие теперь лежат отдельным
  неподвижным списком (`Снаружи.стоят`): их не двигают и не перерисовывают
  каждый кадр, поза — раз на карман; в движение их переводит выход хозяина.
  Стало: 16.6 мс в середине, 21.1 в 95%, худший 28.5. Отрисовка машин —
  партиями с запасом, а не пересозданием при каждом изменении числа.
  «Карман как затор» в жизни улицы теперь невыразим (стоящих нет в списке
  движения) — подделка `затор-в-кармане` снята, правило в `traffic.ts`
  осталось для снимка часа. Город «включён» — отдельным флагом, а не
  «есть ли машины в движении» (ночью все стоят).
- **Шаг 2 сделан: улица живёт по распорядку** (`Снаружи`, `жить` в
  `жизнь.ts`, решение 100). Выход из дома рождает пешехода у подъезда или
  трогает машину хозяина ПО МАРШРУТУ ОТ НЕЁ САМОЙ; приход убирает в здание;
  за город — к концу улицы у края посёлка. Часы города — НАСТОЯЩИЕ
  (`СЕКУНД_В_ЧАСЕ` = 3600; было 60): шаг и часы — одна мера, иначе на
  тротуаре было бы в 58 раз больше людей, чем в пути. Другой час —
  `?час=18`. Часы машин — от начала жизни города, а не по кругу суток
  (по кругу стоящая машина уезжала без хозяина). Найдено и починено по
  дороге: карман считался «затором за перекрёстком» и запирал проезды
  дворов (машина стояла 2689 с); стоящая в кармане мешала выехать со двора.
  Проверка (`life.ts`, 45 мин утра по настоящим часам): пешеходов на улице
  106/101, 202/200, 159/155 (улица/распорядок); выездов на машине 342,
  несостоявшихся 0; дольше всех машина стояла 25 с. Подделка `снимок`
  падает. Снято с уровня глаз на «городе» в 8:06 (`?час=8.1`): улица
  живая, но редкая — 4–5 человек на тротуарах вдали, машины в основном
  во дворах; 428 машин на весь «город».
- **Шаг 1 сделан: распорядок** (`распорядок` в `житель.ts`, решение 099).
  День жителя — дороги подряд от дома до дома: работа или учёба, магазин
  по пути с работы, врач, двор, прогулка; пожилые выходят (до 25.09 не
  выходили никогда, в магазин не ходил никто). Дорога — из расстояния
  и способа (пешком 1.3 м/с, машина 30 км/ч + парковка, ближе 600 м —
  пешком), а не 0.4 ч на всех. Машина стоит там, куда хозяин на ней
  приехал: ушёл пешком в магазин — она ждёт у дома (`гдеМашина`).
  Сверка с обследованием поездок (NTS 2023, `ПОДВИЖНОСТЬ` в `norms.ts`;
  российские сайты из контейнера закрыты): поездок в сутки 2.58 при 2.57;
  за покупками 18.9% при 18%; пожилые в магазин 0.74 в сутки при 0.73,
  к врачу 0.31 при 0.33. Подделка `одно-дело` (как было) — 1.47 поездки,
  0% за покупками: падает. На экране пока то же, что было: видимых людей
  всё ещё столько, сколько «уместно», — это шаг 2.
- **Твердь** (`city/твердь.ts`, решение 098; Алекс: «какого хуя у домов
  нет физики»). Твёрдо то же, что нарисовано, из тех же записей: дом по
  цоколю, крыльцо выше ступени, ствол по породе и масштабу, вещь двора —
  каждой своей частью выше ступени (0.3 м) и ниже роста (1.9 м), столбы
  знаков и светофоров. Кусты мягкие. Машина упирается на КАЖДОМ шаге
  физики (удар — сохранение импульса о бесконечную массу, отскок общий
  с ударом машин), пешеход — кругом плеч 0.3 м со скольжением вдоль стены,
  чужие машины для него тоже твёрдые. Проверка `tools/твердь.ts` (в
  `npm test`): машина в дом на 60 км/ч — 0.0 см внутрь, 57 → 16 км/ч;
  углом в ствол — не сквозь, закрутило до 6 рад/с; пешеход в стену,
  вдоль стены, в лавку — ✓. Подделки: `сквозь` (в дом на 7 м), `по-кадрам`
  (в стену на 14 см) — падают. В браузере на «городе» 16 784 формы;
  пешеход встал у фасада ровно на плечо от цоколя.
  Не сделано: по крыльцу нельзя подняться (двери всё равно не ведут внутрь);
  машина не рушит лавку и столб — только упирается.
- **Замечено на снимке у стены, не разбиралось:** пешком на «городе» небо
  над домами чёрное; рамы окон ближнего дома висят отдельно от стены
  (`shots/твердь-у-стены.png`).

## 25.09, ВЕЧЕР — ДОРОГА НА ГОТОВОМ ГОРОДЕ (Алекс: «20–40 кадров пешком»)

- **На сайте** сборка 25.09 11:36 из 0dc6c24 (всё ниже: мышь пешком,
  курсор строителя, трава до 64 м, плавность). `npm test быстро` ✓;
  браузерные ride, play, глаз, плавность ✓ по отдельности.
- **ОБЪЯВЛЕНА ПЕРЕДЕЛКА: город — из дорог** (правило 9; начинать
  следующей большой работой, если Алекс не скажет СТОП).
  *Что не так.* Город строится по своему плану улиц — прямоугольной сетке
  из зерна и размера (`улицы()` в `plan.ts`). Строитель правит другой
  список — дороги поверхности. Нарисованная дорога проходит сквозь дома,
  дворы и деревья: у улиц две правды (Алекс: «строитель дорог никак
  не совместим с реальным городом, всё ломает» — верно).
  *Цена переделки:* ~10 шагов. Квартал — область, ограниченная ЛЮБЫМИ
  дорогами (многоугольник); участки режутся вдоль его сторон под любым
  углом; двор, проезд, стоянки, тротуар двора — внутри многоугольника.
  Выбросить: всё, что держится на прямоугольниках по осям (x0..x1, z0..z1) —
  около половины `plan.ts` и `двор.ts`. Пересборка при правке — только
  затронутых кварталов, иначе дорога на «большом городе» — минута.
  *Цена НЕ-переделки:* строитель и город несовместимы навсегда, и каждый
  шаг переноса 14.09 (витрины, фонари, остановки, заборы) ляжет на
  прямоугольники — переделка дорожает с каждым.

- **Пешком инструмент «дорога» оставался включён** — при посадке в машину
  его выключали, при выходе пешком забыли. На каждое движение взгляда он
  искал точку земли под курсором: 15.5 мс на «городе», 59 мс на «большом»
  за ОДНО движение мыши (замер в контейнере), а пешком мышь движется
  всегда. Теперь инструмент спрашивает на каждом событии, свободна ли мышь
  (`свободна` в `build.ts`): пешком и за рулём — нет. Стало 0.01 мс.
- **Точку под курсором строителя ищет сама земля** (`GroundIndex.луч`):
  луч шагами по метру спрашивает высоту там же, где колесо. Было — луч
  three.js по всем треугольникам. Сверено на 300 лучах «города» на холмах:
  расхождение точки в середине 2 мм, худшее 12 см; луч 15.6 → 0.07 мс.
  Движение мыши с инструментом в браузере: «город» 16.5 → 0.8 мс,
  «большой» 57 → 2.2 мс (остаток — привязка к сети).
- **Трава видна до 64 м** (было 46) и не кончается чертой. Три причины
  (снято с уровня глаз на холмах «креста» и во дворе «города»):
  газон был своего цвета, светлее и голубее травы — теперь его цвет
  выводится из цвета травинки (`ГАЗОН` в `растения/показ.ts`), газон вдали
  во дворе (62,91,36) при траве у ног (65,89,36); вдали травинки редели
  быстрее, чем ширились, и читались правильными точками — ширина теперь
  до ×6; вдали травинка видна целиком от тёмного корня — к краю дальности
  она целиком цвета верхушки. Окно поля выводится из дальности.
  Цена во дворе: плиток 22 → 36, травинок 307 → 315 тыс.; рывки зелени
  11.4 → 11.5 мс в худшем кадре (шум).
- **Плавность — замер `tools/плавность.mjs`** (в `npm test`, браузер):
  страница пишет по кадру время показа и где камера и машина; дрожь —
  насколько скорость кадра отличается от средней соседей. Было, в худшем
  кадре: машина 98%, камера за машиной 82%, камера пешком 237% — кадры
  через ровные 16.7 мс, а машина за них проезжала от 22 до 53 см. Причин
  две: время кадра брали часами внутри кадра, а не у браузера (момент
  показа), и машину показывали последним шагом физики (в кадр их ложится
  4–6). Теперь время — из requestAnimationFrame, поза машины — между
  двумя последними шагами (решение 097). Стало: 0.2%, 0.1%, 1.8–2.4%.
  Подделка `сломать` (`?плавно=нет`) падает: 74%, 72%, 180%.
  ОГОВОРКА: в контейнере часы внутри кадра скачут сильнее, чем в браузере
  Алекса; у него дрожь той же природы, но меньше — насколько, не знаю.
- **Находка для «пешком недостаточно»:** на бордюре глаз взлетает на 8 см
  за один кадр (проверено: x ≈ −41 на «решётке»). Не чинил.
- **`play.mjs` кликал сквозь панели** — 2 случая из 6 жали «трафик»
  и «треугольник» вместо земли и писали ✓. Теперь такая точка — провал.

## 25.09 — РЫВКИ И РАСТИТЕЛЬНОСТЬ ВДАЛЬ

- **Рывки зелени — замер `node tools/рывки.mjs`** (не в `npm test`, пока
  красный): камера едет по «большому городу» 600 м на 108 км/ч, меряется
  САМЫЙ ДОЛГИЙ кадр работы зелени на процессоре (сам кадр не рисуется,
  `?кадр=нет`). Подделка `сразу` — поле травы в одном кадре, как было:
  худший кадр 145.7 мс, 37 кадров дольше 8 мс. Сейчас: 11.4 мс, 3 кадра из
  1200. Причины найдены и убраны три: поле строилось целиком в кадре
  (теперь порциями по 2.5 мс заранее), упаковка высот в текстуру (тоже
  порциями), большой дом закрывал траву одним куском. Оставшиеся 3 кадра —
  не порции (порция вдвое меньше их не сдвинула); предполагаю сборку мусора
  (~4.5 МБ массивов на каждое поле), не проверено. Потолок 8 мс не опускал.
- **Деревья и кусты растворяются на краю дальности** точками (дизеринг),
  а не появляются целиком: деревья до 230 м, полоса 40 м; кусты до 90 м,
  полоса 25 м; потолок 2048 деревьев. Снято вдоль улицы «большого города».
- **`занято` спрашивает соседей по клетке** (сетка 16 м в `двор.ts`):
  кусты 2.8 с → 0.05 с загрузки, деревья двора 1.3 → 0.02. На GitHub
  24.09 падала «чего стоит зрение» — «большой город» не открывался за 30 с.
- **На сайте было** 25.09 08:16 из 8f421a5; сейчас — см. раздел выше.
- **Дальше (в порядке Алекса):** пешеход и машина «пока недостаточно» —
  нужен разговор, что именно не так на ощупь; перенос 14.09, шаги 2–6
  (балконы, витрины, фонари, остановки, стены-преграды); 3 кадра по 11 мс
  в замере рывков (предполагаю сборку мусора).

## 24.09, ДЕНЬ — ДВОР ПЕРЕСТАЛ БЫТЬ ПУСТЫРЁМ (Алекс: «здания должны знать о городе»)

- **Подъезды жилья городского квартала — во двор.** До 24.09 все 1317 дверей
  «большого города» смотрели на улицу, и в двор никто не выходил. Теперь
  вход решает план (`Объект.вход`): со двора — в городе, у квартала с двором
  и проездом, и только на ту часть задней стены, за которой двор (угловой
  дом смотрит спиной в бок соседнего крыла — входит с улицы). Вход во двор
  без прямоугольника двора записать нельзя: он едет вместе с ним.
  «большой город»: со двора 1129 подъездов, с улицы 159 угловых.
- **Тротуар двора** (`тротуарДвора`) — по краю двора, вдоль задних стен,
  там, где нет проезда (проезд огибает двор только с двух сторон). От каждого
  подъезда к нему прямая дорожка: медиана 7.5 м, наибольшая 17 м, сквозь
  дома 0 (было: колена до 170 м через весь двор и 24 сквозь чужие стены).
- **Крыльцо у каждой двери** (`крыльцо` в `площадка.ts`): площадка у двери
  и ступени (подъём 0.15, проступь 0.30), пока с пола не сойдёшь на землю.
  Прямо от двери — только по газону, иначе вдоль стены, туда, где земля
  ниже. Город: у всех 1651 двери последняя ступень в пределах подъёма
  от земли, на проезжей части ни одной. Деревня на холмах: 11 из 194 выше
  на 0.16–0.21 м — кочка сразу за лестницей.
- **Проезд двора сдвинут во двор на полширины.** Его ось лежала на задней
  границе участков, и ближняя половина — тротуар, стоянка, полоса — заходила
  на участки на 5.5 м, до самой задней стены. Теперь край его тротуара —
  граница участков.
- **Перед подъездом со двора — площадка в плитке** 4 × 3.5 м: кусты и трава
  её обходят (утром сирень выросла прямо перед дверью).
- **Двор наполнен** (`вещиДвора`, модели — `src/модели.ts`): детская площадка
  на резиновом покрытии с горкой, качелями, песочницей с грибком, каруселью
  и лавками; хоккейная коробка с турниками; беседка со столом и лавками;
  сушилка для белья; у каждого подъезда со двора лавка и клумба; тропинки
  от площадок к тротуару двора; деревья двора рощицами (5 м от стен, СП).
  Места выбираются из свободного, зоны — врозь. «большой город»: 36 коробок,
  36 беседок, 1262 лавки, 1121 клумба, 2534 дерева во дворах; расстановка
  0.86 с (первая редакция спрашивала о всём городе на каждую пробу — 69 с).
  Вещь одного вида — одна пачка, форма, а не коробка (Алекс: «странное
  строение из коробок»).
- **Разметка стоянок** — из тех же карманов сети, куда встают машины.
- **Место проёма — одно на проект** (`гдеПроём`, `осиПроёма` в `дом.ts`):
  показ, дорожки и жители раньше считали дверь каждый по-своему.
- **Крыша** закрыта снизу, торец — фронтон цвета стены (было: тёмный
  треугольник, из-под свеса просвечивало).

## 24.09 — ВЕТКИ: ПОЧЕМУ ТЕРЯЛАСЬ РАБОТА И ЧТО ТЕПЕРЬ (решение 095)

- **`main` как ветка по умолчанию ничего не чинил.** Новый чат начинается
  с `main`, а `main` никто не двигал: он стоит на 14.09, свежая работа —
  в `claude/practical-hamilton-fig7t7` (впереди на 58).
- **Проверка на GitHub не выполнилась ни разу — проверено.** 32 запуска
  с 15.09 — «провал» в ту же секунду, без единой работы: имена работ
  были русскими, и GitHub не мог прочитать файл. Имена стали латинскими
  (`bystro`, `brauzer`), с 24.09 обе работы запускаются.
- **Отчёт начала чата** (`tools/ветки.mjs`, крючок SessionStart): что лежит
  в ветках и не дошло до `main`. Так мир видел бы новый чат, начатый
  с `main`: четыре невлитые ветки. Раньше не видел ни одной.
- **Крючок конца хода** (`ветки.mjs догнать`) двигает `main` вперёд и назад
  не умеет. Проверен на подставном GitHub; **включён 24.09 по «да» Алекса**,
  `main` перемотан с 14.09 на свежую работу.
- **Работа 14.09 (`friendly-ritchie`) — не слияние, а перенос.** Это
  параллельная постройка того же города от той же точки: 5000 строк
  в `house.ts`, `street.ts`, `walls.ts`, `plan.ts`. `git merge` столкнёт
  два разных `plan.ts`. Переносить по частям, начиная с того, чего здесь
  нет вовсе, и сверяя снимками с уровня глаз. Коллаж «было 14.09 / стало
  24.09» с тротуара: у 14.09 окна в глубину, горящие окна, балконы, витрины
  с залом, козырёк, лестница за дверью; у 24.09 окна нарисованы на коробке.
  **Шаги переноса** (каждый кончается снимком с тротуара; код 14.09 —
  образец и готовые числа, не копия: там другая модель дома):
  1. окна в глубину и часть окон горит; 2. балконы; 3. витрина магазина,
  козырёк, зал за стеклом; 4. фонари вдоль улиц; 5. остановки, заборы,
  лавки у подъездов; 6. стена — преграда, в дом через дверь.
  После переноса ветка 14.09 сохраняется в `archive/` и удаляется.
  **Шаг 1 сделан (24.09):** рама с откосом, подоконник и импост у каждого
  окна; стекло по высоте от «дома напротив» к небу; горит 2027 окон из
  18 304 (11.1%, от места окна, не от номера); над дверью козырёк. Рамы
  рисуются в радиусе 100 м от камеры: на всех окнах кадр тяжелел на 36%
  (2.62 → 3.56 млн треугольников), с радиусом — на 4% (2.72 млн).
  По дороге найдено: с 15.09 каждая дверь висела над полом на 1.15 м —
  показу передавали середину проёма вместо низа.
  Дальше: крыльцо (пол над тротуаром до метра, ступеней нет), шаг 2 — балконы.
- **Выкладка 24.09 в 05:20 отправила на сайт вчерашний мир** (сборка 23.09
  16:57 из 56b5311) с подписью «из e2944fe» — Алекс увидел, что ничего нового
  нет. Выкладка брала готовый файл с диска, а `npm run site` не запускали.
  Теперь `deploy` собирает мир сам; с незакоммиченными правками отказывает
  (показано: «НЕ ВЫЛОЖЕНО», `gh-pages` не сдвинулся).
- **Поездка в браузере на GitHub падала в одном прогоне из трёх.** Журнал
  показывал хвост из 12 строк, упавшая строка была выше. Теперь печатаются
  все ✗ (`tools/all.mjs`), и следующий провал назвал себя: «руль прям, пока
  мышь не трогали: −100%». Корень в `src/aim.ts`. Прыжок курсора при захвате
  выбрасывался как «первое событие после pointerlockchange», а браузер
  присылает его одним или двумя событиями, до смены захвата или после.
  Теперь после захвата 150 мс тишины, а сам захват замечает тот, кто узнал
  первым. На подделке из четырёх порядков старый код ошибался в двух: в одном
  проглотил движение игрока, в другом пропустил прыжок. Новый ошибок не даёт.
  На GitHub до починки браузерная половина упала в 2 прогонах из 6
  (33–38), после починки 2 из 2 зелёные (39, 40). Для уверенности этого
  мало: следить за следующими.

## 23.09 — ТРАВА, ДЕРЕВЬЯ, КУСТЫ (Алекс: «графику днём делает растительность»)

- **На сайте с 17:02 (`48a7f19`).** Выкладка проверила в собранном файле:
  141 тыс. травинок, 387 деревьев, 344 тыс. листьев в кадре двора; разгон
  до 66 км/ч. Панель: «трава · T», «листва · L» (прямоугольник / ромб /
  нет), «ветер · V»; рядом с травой — кадры в секунду.
- **Травинка и лист сняты со стола.** 24.09 Алекс: «оставь всё пока так —
  все варианты нравятся». Остаются умолчания (`остриё`, прямоугольник), кнопки
  на месте; «не так» можно сказать в любой день.
- **Ролики снимаются из собранного файла** (`npm run site`), кадр — с холста
  в том же кадре, где нарисован: снимок Playwright тратил 30 с из 39 на
  кадр. Скрипт съёмки — в черновиках сессии, в проект не входит.

- **Что есть.** Трава колышется от ветра волнами порывов и мнётся под
  ногами и колёсами. Четыре варианта травинки при равной цене в вершинах:
  `остриё` (по умолчанию), `треугольник`, `полоска`, `гладкая`. Кнопки
  «трава» и «ветер» в панели, клавиши T и V, адрес `?трава=…&ветер=…&густота=…`.
  Рядом с кнопкой травы — кадры в секунду и сколько тысяч травинок в кадре.
- **Устройство — решение 093.** Травинка не хранится: номер в плитке 12 м →
  точка R2. Где расти — `src/растения/поле.ts` (клетка 25 см, все четыре угла
  в траве и не заняты). Рисует `src/растения/показ.ts`. Мир про траву не знает.
- **Проверка** — `npm run трава`: трава только на голой земле, газон покрыт
  на 98.8%, корень на земле (≤ 3 см, почему не 1 — решение 093), след один
  на 30 и 144 кадрах. Три подделки падают: маска по одному углу, трава сквозь
  дорожки, примятость по кадрам.
- **Браузерные проверки смотрят без травы** (`&трава=нет`): `ride`, `play`,
  `глаз` меряют не её, а кадр с травой на программном отрисовщике — секунды.
  Траву в браузере проверяет выкладка: страница с травой, шейдер собрался,
  травинки растут. Снимок (`shot`) падает, если шейдер не собрался.
- **ЛОВУШКИ этого дня.** `pgrep -f` не находит процессы с русскими буквами
  в пути — «процесс завершён» было ложью; ждать по номеру процесса. В пешем
  режиме по умолчанию включён «глаз», и кадр с травой в контейнере — 12 с;
  для съёмки жать O. Недобитый браузер держит порт 5199.
- **Деревья — из веток (решение 094).** Пять пород (берёза, липа, клён,
  тополь, рябина) и два куста (сирень, изгородь) растут скелетом в
  `src/растения/дерево.ts`, рисуются в `деревья.ts`. Проверка `npm run деревья`.
  Утренняя «листва на шаре» (`листва.ts`) удалена: Алекс — «шар с листьями».
- **Кусты** (`city/зелень.ts: кустыДворов`): сирень группами у дворовой стены
  дома, изгородь вокруг детской площадки с двумя проходами. Где можно —
  одна проверка на страницу и на `npm run деревья`: трава, не занято,
  не вплотную к дереву. В городе 642 сирени и 832 куста изгороди; дальше
  70 м кусты не рисуются.
- **Дыры деревьев.** Листьев в кадре двора 344 тыс. — это ~1.4 млн вершин
  сверх травы; на настоящей видеокарте не замерено. Дальние деревья
  растворяются к 230 м (потолок 2048), кусты к 90 м — раздел 25.09.
  Крона пока не знает стен: ветки у фасада могут войти в дом (шаг 4
  переделки — порода по месту — не сделан).
- **Дыры.** Цена на настоящей видеокарте НЕ замерена (в контейнере её нет):
  ~2 млн вершин травы в кадре двора; живых травинок меньше обработанных —
  плитка считается по ближнему краю. Окно поля 176 м перестраивается
  на ходу за ~25 мс — на машине это рывок раз в пару секунд. Колёса мнут
  траву — написано, в браузере не проверено.

---

## 22.09, ВЕЧЕР — ПРОЧИТАТЬ ДО ЛЮБОЙ РАБОТЫ

1. **Работа терялась между чатами — проверено.** `main` стоит на 14.09
   и отстаёт от свежей работы на 44 коммита. Ветка
   `claude/friendly-ritchie-csb21o` (14.09, 18 коммитов: квартал по периметру,
   дом-оболочка с проёмами, двор с площадкой и парковкой, фонари, деревья,
   балконы, горящие окна) не влита никуда, и с 15 по 22.09 то же самое
   строили заново. Ракурс `road` сцены «город» с одной точки: у 14.09 кадр
   богаче, чем у 22.09. Корень: «в конце чата влить в `main`» (решение 039)
   держится на памяти, а Клод в облаке пушит только в свою ветку. Предложено
   сделать потерю невыразимой — `main` двигает сам GitHub. Ждёт «да» Алекса:
   это разрешение, а не техника.
2. **Самая свежая правда — `claude/practical-hamilton-fig7t7`**
   (= `gifted-mayer` + набросок стилей). Пока `main` не догнан, новый чат
   начинает отсюда. Первый шаг после «да»: свести 14.09 и 22.09 в одну
   ветку, выбирая лучшее по снимкам с уровня глаз.
3. **Набросок стилей — `src/стиль.ts`.** Включается только адресом
   `?стиль=свет|пиксели|камера`; без параметра кадр тот же, что был.
   Геометрия та же, меняются свет и обработка кадра. Три снимка одной улицы
   показаны Алексу, выбирает он. Это набросок, а не решение: после выбора
   стиль делается как следует, а файл переписывается или удаляется.
4. **Главный вывод разбора:** 15 дней мерили «правильно ли» (ПДД, нормы,
   уклоны), а Алекс смотрит «красиво ли и интересно ли». Два вечерних
   прогона со светом дали больше видимой разницы, чем неделя работы над
   дворами. Дальше срез, а не ширина: одно место до вида и ощущения
   настоящей игры, потом расширять.

---

## ЧТО НА САМОМ ДЕЛЕ ЖЖЁТ КОНТЕКСТ

Не файлы. Файл читается один раз и стоит копейки — и он ровно затем и нужен,
чтобы Клод не делал глупостей, за которые потом платит контекстом.

Жжёт **диалог**, и вот чем — по этой сессии, с ценой:

- **переделка из-за «сделал не глядя».** Застройку выложили на сайт после
  одного снимка сверху; с улицы оказался склад контейнеров, и весь круг —
  снять, объяснить, переделать — пошёл заново;
- **слепой перебор вместо опыта.** Выцветание кадра искал пятью прогонами
  подряд, меняя по два параметра. Нашлось одним опытом: собрать цепочку
  вообще без эффектов и посмотреть. Надо было начать с него;
- **переписывание больших файлов скриптом.** Файл после этого возвращается
  в переписку целиком. Точечная правка стоит в разы дешевле;
- **лишние снимки.** Картинка стоит как страница текста. Снимок делается,
  когда он что-то РЕШАЕТ, и сразу с того ракурса, где видно правду;
- **длинные выводы.** Полный прогон вместо хвоста, `grep` без ограничения;
- **перечитывание своего же.** То, что сам только что написал, читать незачем.

**Правила, которые из этого следуют:**

1. Прежде чем делать — вслух назвать, чем проверю. Не назвал — будет переделка.
2. Один опыт отвечает на один вопрос. «Поменяю три штуки и посмотрю» —
   это не опыт, это трата.
3. Правки точечные. Переписывание файла целиком — только когда файл меняется
   целиком.
4. Снимок — с того места, где стоит игрок, и один, а не серия.
5. Выводы обрезать хвостом.
6. Числа не помнить, а запускать: `npm test быстро` — 17 минут в контейнере
   (замер 22.09, 43 из 43; жители одни 2.5 мин). Было записано «полторы» — врало.

И главное: **эта память нужна не для экономии, а против повторения ошибок.**
Ловушки, запреты, инварианты и замеренные числа отсюда не выкидываются —
они дешевле любой переделки, которую предотвращают.

---

## НАЧАЛО НОВОГО ЧАТА — прочитать это, и хватит

Пять строк по правилу 13, чтобы не изучать проект с нуля:

1. **Где мы.** Есть мир с дорогами, машина с честной физикой, город по ПДД
   (светофоры, очередь, приоритет, знаки, полосы, пешеходы, удары) — и человек,
   который может выйти из машины и пойти пешком. Всё это живёт на сайте.
2. **Что работает.** Город и машина проверяются без экрана (`traffic`,
   `drive`, `car`, `scenes`, `fuzz`) и в настоящем браузере (`ride`, `play`).
   У каждого инструмента есть заведомо сломанный вариант, на котором он
   ОБЯЗАН упасть, — по правилу 11 зелёная галочка до этого ничего не значит.
   Сколько сейчас проверок и какие числа — смотреть запуском, а не здесь.
3. **КВАРТАЛ ТЕПЕРЬ ПЕРИМЕТРАЛЬНЫЙ (21.09).** Угол квартала застроен,
   фасад стоит на красной линии, дома в городе смыкаются в сплошную стену,
   красная линия выведена из ширины улицы. Под застройкой 15.8% → 25.5%
   при настоящих 25.8 (решение 086). Улица с уровня глаз наконец читается
   городской, а не загородной.

   **Правило, которое это оставило после себя:** нормативы могут быть
   выполнены все до одного, а города не быть. `plan` проверял норматив,
   `морфология` — форму, и оба были зелёными, пока с улицы стоял склад
   контейнеров. Мера, которая это поймала, — «под застройкой», и она же
   осталась главной.

3б. **ВО ДВОРЕ ПОЯВИЛОСЬ СОДЕРЖИМОЕ (22.09).** Площадка, горка, качели,
   четыре лавки вокруг неё и баки у проезда — 36 дворов «города», 274 вещи,
   на проезжей части 0. Ни у одной вещи нет своих координат: место площадки
   ВЫБИРАЕТСЯ перебором свободных точек двора, остальное выводится из неё
   и из проезда. Тем же местом наводится ракурс «двор», поэтому «камера
   смотрит в одно, а стоит в другом» записать негде (решение 090).

3в. **У ДОМА ЧЕТЫРЕ СТЕНЫ (22.09).** Дворовой грани в данных не было
   вовсе: окна резались на фасаде и двух торцах, то есть только там,
   где видно с улицы. Двор был обнесён забором из собственных домов.
   Окон 53 171 → 75 355, домов с глухой стеной 1018 → 0 (решение 091).
   **Нашёл это снимок, а не проверка:** «окна внутри стен» спрашивала,
   не вылезло ли окно за угол, а пустая стена её устраивала полностью.

3a. **Что следующее.** Город снаружи стал городом: дома садятся на рельеф,
   кварталы обзавелись дворами, въездами, деревьями и подходами к подъездам,
   дома перестали быть одинаковыми, во дворах появились стоянки, и дворы
   наконец СОЕДИНЕНЫ с улицами (до 18.09 все до одного были островами).
   `npm run морфология` показывает, сколько осталось до настоящего: разброс
   следов 6.5× против 12.1, под застройкой 15.7–16% против 26%, квартал
   200 м против 120. Дальше по порядку — **ЗАСТРОИТЬ УГЛЫ** (см. ниже,
   это главный рычаг и он замерен), **разные ТИПЫ застройки** (магазин,
   гараж, многоквартирный дом — их следы отличаются на порядок, и это то,
   чем добирается разброс), **страна как параметр генерации** (просьба
   Алекса от 17.09).

   **ЗДЕСЬ СТОЯЛО «квартал вдвое мельче поднимет разом три меры» — ЭТО
   БЫЛО НЕВЕРНО, и 18.09 замерено.** Поставил `шаг: 120` и запустил
   морфологию: отрезок улицы стал ровно 120 м (настоящий), плотность улиц
   28 353 против настоящих 22 767 — уже с перебором, а **застройка УПАЛА
   с 16.3% до 10.5%**, медиана следа с 336 до 190 м². Откачено.

   **Почему упало и где настоящий рычаг.** Замерено на посёлке 600 м:
   дом занимает 86% фронта своего участка (промежутки между домами всего
   3.2 м) и 57% его глубины (14.7 м из 26). То есть периметр застроен
   плотно, а не дырявый. Не хватает другого: **углы кварталов пустуют.**
   `уголОт` отступает ГЛУБИНУ УЧАСТКА (26 м) от каждого конца каждой
   стороны — чтобы участки поперечных сторон не налезли друг на друга.
   На квартале в 200 м это съедает 52 м из 200 (26%), на квартале в 120 м —
   52 из 120 (43%), и потому мельчить бесполезно, пока углы пустые.
   В настоящем квартале угол ЗАСТРОЕН, и это классический угловой дом
   с магазином. Застроить углы — и рычаг «квартал мельче» заработает.
4. **Чего не трогаю.** Физику и геометрию дороги (`src/world/`,
   `src/surface/`) — запрет Алекса от 8 сентября, он в силе. Читать можно,
   писать нельзя без его слова.
5. **Что нужно от Алекса** — раздел «Стол Алекса» ниже, не больше трёх
   пунктов. Всё остальное Клод решает сам и показывает результат.

**Ветки — решение 039.** Правда одна и называется `main`; она же ветка
по умолчанию, с неё начинается любой новый чат. Чат работает в своей ветке
`claude/*` и в конце вливается в `main`, после чего его ветка удаляется.
`gh-pages` — сам сайт, его двигает только `npm run deploy`.
`archive/city-v1` — город v1 до переписывания, лежит и не трогается.
**Ветка не удаляется, пока не посмотрели, что внутри, и пока её вершина
не сохранена где-то ещё.**

**Дальше по файлу:** стол Алекса → что построено → инварианты → чем
проверяется → дыры → папки → куда идём. Журнал решений, ограничивающих
будущее, — в `docs/decisions.md` (сейчас их 53, последние — про город
и интерьеры). Целиком он не читается никогда: только нужная запись по номеру.

---

## Стол Алекса: три пункта (правило 15 — не больше трёх)

По правилу 15 сюда попадает только вкус, ощущение, направление — и
разрешение там, где его требует среда. Технику Клод решает сам.

1. **Интерьеры — нужны ли** (24.09, Алекс: «как в San Andreas, отдельным
   измерением при входе; но не знаю пока, насколько это нужно»). Направление,
   решает Алекс; до его слова дом снаружи.
2. **Стало ли ровнее на ощупь** (25.09): время кадра и поза машины
   починены (`плавность.mjs`: дрожь машины 98% → 0.2%), но у Клода замер
   только в контейнере. Ровно ли теперь едет и идётся у Алекса — ощущение,
   его не замерить. (Кадров у Алекса 59–60; просадка до 20–40 была от
   инструмента «дорога», оставшегося включённым пешком, — починено.)
3. **За рулём:** какая подвеска живая (`P`: дорога 1.5 Гц / спорт 2.0 /
   трек 2.7, сейчас спорт) и те ли 700 пикселей на полный выворот (`[` `]`).
   Числа взяты у Assetto Corsa, рукой не проверены.

### Снято со стола — решил Клод (правило 15)

Здесь висели девять технических вопросов; ни один не был про вкус. Все решены,
у каждого названа цена отмены — **решение 042 в `docs/decisions.md`**. Коротко:
перекрёсток кончается там, где сказала дорога; вариант дороги А принят; физика
машины своя; рельеф остаётся заглушкой; крутой поворот разгибается, но не молча;
инварианты машины подтверждения не требуют; «почему видно полигоны» объяснено.

---

## Что построено — карта, подробности в справочниках

| что | где код | где подробно |
|---|---|---|
| мир, рельеф, дороги, поверхность | `src/world/`, `src/surface/` | `docs/how-roads-work.md` |
| машина: паспорт, шина, опора, физика, руль | `src/car/` | `docs/how-cars-work.md` |
| город по ПДД: трафик, светофоры, знаки, полосы, пешеходы | `src/city/` | `docs/how-traffic-works.md` |
| посёлок по нормам: улицы, КВАРТАЛЫ, дворы, въезды, участки, население | `src/city/plan.ts`, `norms.ts` | `docs/how-cities-work.md` |
| как дом встречается с землёй: площадка, пол, цоколь | `src/city/площадка.ts` | решение 069 |
| деревья улиц и дворов | `src/city/зелень.ts` | решение 073 |
| человек на ногах: тело, шея, глаза, моргание, глаз-картинка | `src/person/`, `src/aim.ts` | `docs/how-presence-works.md` |

**Запрет в силе:** физику и геометрию дороги (`src/world/`, `src/surface/`)
не трогать без слова Алекса. Читать можно, писать нельзя.

**Вариант дороги А — «одна поверхность».** Плоскость режется на треугольники
один раз, покрытие решается по тому, куда попал центр треугольника. Этим
невыразимы: щель между покрытиями, земля сквозь асфальт, торчащий тротуар,
треугольник изнанкой вверх.

## Инварианты

### Мира и дороги

1. Метр — единственная единица. Мир живёт рядом с нулём. Игровой сетки нет.
2. Земля — одна поверхность. Дорога — её участок с другим покрытием
   (в варианте А выполнено по построению; Б это нарушает намеренно).
3. Дорожная сеть — граф. Видимая геометрия вычисляется из графа.
4. Мир не знает, что его рисуют. Считается без экрана.
5. «Существует» и «видно» — разные вещи.
6. Мир не знает, откуда взялись данные.

Целость меряется **по местам рёбер**, а не по номерам вершин: две вершины
в одной точке — это излом (бордюр, край выемки), а не щель.

**Предельный уклон — свойство ПОЛОТНА, а не осевой линии.** Две точки полотна,
отстоящие на d метров, различаются по высоте не больше чем на d × 8% — вдоль,
поперёк, между разными дорогами, всё равно. Мерится зазор между краями
полотна, а не между осями (решение 015).

**У дороги два предела, а не один: по подъёму и по повороту.** Радиус поворота
не меньше трёх полуширин полотна (решение 016). Кривая через опорные точки —
центростремительная: у неё доказано, что петель и изломов не бывает. Станции
идут через ровные два метра, иначе радиус из трёх соседних точек ничего
не значит.

### Машины — НУЖНО ПОДТВЕРЖДЕНИЕ АЛЕКСА

1. **Машина спрашивает ту же поверхность, которую видит игрок.** Отдельной
   «физической» земли не существует.
2. **Сила рождается только в четырёх пятнах контакта.** Ни одного правила
   вида «если занос — то...».
3. **Всё в метрах, килограммах, секундах и радианах.** Лошадиные силы, мили
   и обороты — только на экране.
4. **Число шагов физики в секунду не зависит от числа кадров.** Иначе на
   слабом компьютере машина поедет иначе — а нам в мультиплеер.
5. **Уровень подробности машины меняется, состояние — нет.** Дальняя машина
   считается грубее, но её положение и скорость — те же самые числа.

### Человека на своих двоих

1. **Камера — это глаза, а не вещь.** Её положение и поворот берутся из тела
   целиком; показ не добавляет ни тряски, ни поворота от себя.
2. **Земля под ногами — та же, что под колесом и на экране.** Один `GroundIndex`
   на всех. Второй земли не существует.
3. **Взгляд ведут глаза, догоняет голова, доворачивает тело.** Только в этом
   порядке; перескочить через звено нельзя.
4. **Всё, что трясётся, моргает и запаздывает, живёт во времени, а не в кадрах.**
   Иначе на слабом компьютере получается другой человек.

### Города

0. **Квартал — это ОБЛАСТЬ, а не шаг сетки.** Участки нарезаются из квартала
   по периметру; двор — это то, что осталось в середине, и он вычисляется,
   а не хранится. Въезд не ставится во двор, а вырезается из периметра.
   Поэтому «участок без квартала», «двор, рассогласованный с застройкой»
   и «двор без въезда» записать негде (решение 071).
0a. **Дом встречается с землёй в одном месте, и это ПЛОЩАДКА, а не точка.**
   Пол — это самая высокая земля под следом плюс порог, подошва — самая
   низкая минус запас, цоколь между ними вычисляется. «Угол висит»,
   «стена вросла» и «пол с уклоном» невыразимы (решение 069).
0b. **Проезд во дворе — это ДОРОГА**, а не нарисованный асфальт. Два вида
   дороги — настоящая и картинка — в проекте невыразимы (решение 072).
0c1. **Выезжающему со стоянки всегда есть куда деваться:** полоса занята —
   он возвращается в карман, а не замирает поперёк неё (решение 084).
0c. **Место, где машина СТОИТ, — не полоса, по которой ездят.** Карман
   выводится из полосы вида `parking` и ниоткуда больше; полосы движения
   берутся только из `travel`. Поэтому «стоящая машина перегородила
   движение» записать негде — и объезд стоящей по встречной не нужен
   как механизм (решение 080).
0e. **Светофор — принадлежность УЛИЦ.** Он ставится там, где есть
   магистральная и где все подходы — улицы. Улица отличается от
   внутриквартального проезда осевой (разметка или разделительная полоса);
   определение одно на весь проект. Поэтому «светофор у выезда из двора»
   невыразимо, а не отлавливается (решение 082).
0f. **Старшинство дороги меряется полосами ДВИЖЕНИЯ, а не шириной асфальта,
   и главная — это РАНГ, а не одна дорога.** Иначе проезд со стоянками
   старше улицы, а улица, разрезанная узлом, уступает сама себе.
0g. **Объект берёт участки только из СВОЕГО СПЛОШНОГО КУСКА застройки,
   а его середина и фронт выведены из одних и тех же краёв занятого
   отрезка.** Поэтому «школа поперёк въезда во двор» и «здание шире, чем
   заняло» невыразимы (решение 083).
0d. **Проезд начинается на ОСИ УЛИЦЫ.** Устье вычисляется проекцией въезда
   на ось его улицы, а не ставится на красной линии. Поэтому «двор, в который
   ведёт нарисованный въезд, но нет дороги» невыразимо; связность при этом
   проверяется у СЕТИ, а не у застройки.

1. **Одно определение столкновения на весь проект** — `touching` (решение 028).
   Им решает и город («можно ли ехать»), и проверка («столкнулись ли»).
2. **Место в дорожной сети вычисляется, а не хранится** — и у игрока,
   и у сбитой машины. Хранимое разошлось бы с настоящим положением.
3. **Правило пишется один раз.** Проезд на красный спрашивается одним `watch`
   и чужой машиной, и наблюдателем за игроком.
4. **Решение не зависит от порядка обхода.** Всё, что зависит от других машин,
   считается в предпроходе, до того как кто-либо тронулся.

---

## Чем проверяется

**Числа здесь не переписываются руками — они живут в выводе инструментов.**
В этом файле три дня стояло «0 сломанных построек»; цифра врала, и никто её
не перепроверял. Нужно число — запусти и прочитай. Здесь только то, что
запуском не узнаешь: что каждая проверка ловит и на чём доказано, что она
не врёт (правило 11).

| проверка | что ловит | чем доказана, что не врёт |
|---|---|---|
| **`npm test`** | **всё сразу**: двенадцать проверок, которые обязаны пройти, двадцать заведомо сломанных случаев, которые обязаны упасть, плюс две браузерные. Весь прогон без браузера — около 11 минут, браузерная половина — ещё три. `npm test браузер` — только она. Заведомо сломанный случай, который прошёл, — провал всей проверки, а не мелочь. `npm test быстро` — то же без браузера | занизил мощность мотора на 20% — «машина на бумаге» упала, код выхода 1, виновный назван. Долг по обстрелу (56 известных поломок, дыра 0) — потолок, а не цель: станет больше, упадёт |
| `npm run traffic` | город без экрана, две минуты жизни; на сцене с посёлком машины берутся ИЗ ЖИТЕЛЕЙ, на голой — ничьи, и они не паркуются вовсе. Проверяется: красный, наложение габаритов, **стоящие не перекрывают полосу движения**, пешеходы на проезжей части, переходы, парковки, поворотники, импульс при ударе, ПДД игрока | заведомо сломанные варианты считаются тут же и ОБЯЗАНЫ провалиться, называя виновных поимённо. `решётка сломать` выключает дистанцию, `без-правил` — правила, `город стоянка-в-полосе` возвращает карман на полосу движения (258 480 случаев, заход на всю ширину кузова) |
| `npm run ride` | то же в настоящем браузере плюс руль мышью и помощь рулю; снимки в `shots/` | разгон сверяется с расчётом из терминала: браузер и мир без экрана обязаны сойтись. **Её слабость названа: половина утверждений — про то, что СЛУЧИЛОСЬ за сорок пять секунд живой езды** (догнал ли кого-то, ударил ли, выехал ли на встречную). Такие утверждения переворачиваются от любой перестановки в городе, даже когда город стал лучше. Пока это лучшее, что есть про живую страницу; гонять её обязан CI, а не память |
| `npm run drive` | машина без экрана против замеров настоящего Viper | `drive сломать` обнуляет высоту центра масс: промах и падение |
| `npm run car` | паспорт Viper → поведение на бумаге, до всякой физики | `car сломать` убирает перенос веса: промах и падение |
| `npm run scenes` | 12 сложных сцен × 3 рельефа: дырки, изнанка, плоские треугольники, пересечения границ, уклон | `scenes -- B` («лента и земля порознь») даёт незашитые рёбра и землю сквозь асфальт |
| `npm run fuzz N` | N случайных сетей: полотно круче предела, то есть ступенька вместо дороги | сломанные зёрна повторяются от запуска к запуску; их номера — в «дырах» |
| `npm run check break` | незашитые рёбра | с выключенным бордюром ОБЯЗАН их найти и упасть |
| `npm run deploy` | выкладка на depth4.github.io через `gh-pages` | мир СОБИРАЕТ САМ из текущего коммита (24.09 выложил вчерашнюю сборку, забытую на диске), с незакоммиченными правками отказывает; перед выкладкой открывает собранный файл и проезжает в нём. Проверено на заведомо сломанном: отказывает и ветку не двигает |
| `npm run play` | шесть сценариев настоящими кликами мышью | |
| `npm run section крест mountain -38` | разрез поперёк дороги: было / стало / откос | |
| `npm run shot ...` | снимки из терминала, пачкой, в любой ракурс | |
| `site` / `page` / `report` | мир одним файлом, страница сравнения, сводка в JSON | цифры на странице считаются каждый раз, а не переписываются, и снимки тоже: `page` сам снимает все 14 кадров в одном браузере. Проверено на сломанном — стёр один кадр и запустил `npm run page быстро`: отказ, код 1, имя названо. **Но эта проверка доказывает только, что кадр ЕСТЬ, а не что на нём то, что в подписи** — на кадры надо смотреть глазами, и 16 сентября половина из них оказалась не про то |
| `npm run load` | **сколько город тянет и как растёт**: мс на шаг при разном числе жителей (лестница до 3200), плюс цена жителя, которого не считают, а вычисляют | считает по факту, а не по просьбе: на «решётке» встаёт 51 машина вместо запрошенных 800, и таблица это показывает. Первая же редакция врала ровно потому, что верила просьбе. Вторая врала иначе: печатала рост, когда машины упёрлись, а росли одни пешеходы — теперь на «решётке» в этой колонке стоит «машины упёрлись — рост не про них» |
| `npm run life` | **жители: дом, дело, машина, распорядок, пешком.** Каждый ли где-то живёт, ходит ли ребёнок в БЛИЖАЙШИЙ сад, сходится ли возрастной состав с нормой, не теряется ли кто-нибудь за сутки; и главное — у каждой стоящей машины есть хозяин, он внутри, и она уезжает в час его выхода | два заведомо сломанных: `сломать` раздаёт детям случайные сады (падает радиус), `выезд-по-своей` возвращает выезд из кармана по своей же полосе (зависает 121 машина из 149), `пешком-наугад` отбирает у пешехода маршрут (доходят 0 из 400 вместо 392) |
| `npm run walk` | **пешеход без экрана**: шея кончается и тело доворачивает, шаг считается путём, остановка с весом, глаза ведут, моргание по-человечески, глаза держат высоту над землёй | `npm run walk сломать` снимает предел шеи: падают ровно две проверки про шею и разворот |
| `npm run трава` | **трава без экрана**: растёт только на голой земле (не на асфальте, дорожке, под вещью), газон покрыт, корень на земле, след одинаков на 30 и 144 кадрах | `маска-по-центру`, `сквозь-дорожки`, `по-кадрам` — каждая падает, называя виновного |
| `npm run inside` | **цена интерьера**: сколько стоит дом, в который можно войти, против дома-коробки. Меряет время кадра, треугольники и вызовы отрисовки в настоящем браузере | замер идёт на программном отрисовщике — абсолютные миллисекунды это нижняя граница, а не приговор; смысл в том, как растёт цена |
| `npm run plan` | **план посёлка по нормам**: радиусы до сада, школы, магазина и остановки; мест не меньше нормы; застройка не в красной линии и стоит там, где велит норма отступа; участки не налезают; у каждого двора есть въезд и он ведёт внутрь; каждый участок ровно в одном квартале; машиноместа помещаются во двор; один сид — один посёлок. `npm run plan карта` рисует план картинкой. **И связность: до каждой дороги посёлка можно доехать** — это свойство СЕТИ, а не застройки, и спрашивается у сети. **И проём: в разрыве под въезд не стоит дом** — это свойство ЗАСТРОЙКИ, и спрашивается у неё, а не у записи о въезде | `npm run plan сломать` ставит объекты куда попало: падают ровно проверки радиусов. Первая редакция подделки НЕ падала — проверку чинил первой. Ещё семь подделок: `кварталы-от-конца`, `дом-шире-участка`, `без-разбора`, `палисадник-наоборот`, `двор-без-въезда`, `двор-без-устья`, `смежность-по-расстоянию` |
| **`npm run морфология`** | **похож ли город на НАСТОЯЩИЙ — числом.** `plan` проверяет норматив, а норматив выполняется и складом одинаковых ящиков. Здесь меряется форма застройки и сверяется с замером настоящего квартала (`ОБРАЗЕЦ` в `norms.ts`, Прага/Бубенеч; метод — `docs/how-cities-work.md` §6): разброс следов домов, медиана следа, вытянутость, доля под застройкой, плотность улиц, размер квартала. Плюс «дом и земля» (под домом нет щели, дом не врос) и «дерево стоит в газоне» | три подделки, и каждая валит свою меру: `одинаковые` — все дома одного размера, `по-центру` — земля спрашивается в одной точке (это ровно поведение до 17.09: 179 домов из 180 висят), `дерево-на-дороге` — деревья на полотне. Зелень меряется и на «каше»: развилка под острым углом — единственное место, где отступ от своей улицы попадает внутрь поперечной, и без неё эта подделка перестала ломаться вовсе |
| `npm run types` | **типы и страницы, и инструментов.** `tools/` запускается через `--experimental-strip-types`, а он типы СРЕЗАЕТ, не проверяя; полгода туда никто не смотрел | `npm run types сломать` подкладывает файл, где число выдано за строку: обязан поймать. На нём и нашлись четыре настоящие ошибки в `tools/` |
| `npm run dev` | страница на http://localhost:5199 | |

**Живую страницу из контейнера Клод открыть не может** — прокси блокирует
`depth4.github.io`. Поэтому проверяется собранный файл до выкладки и сверяется
хэш после. Что GitHub его раздал — Алекс видит сам. В углу страницы стоит
отметка сборки: по ней видно, свежую версию открыли или старую из памяти
браузера.

**Ловушка, найденная 17 сентября: УБИТЫЙ ПРОГОН ОСТАВЛЯЕТ ЖИВОЙ БРАУЗЕР.**
Прервали `npm test` по таймауту — `headless_shell` остаётся и держит
до тринадцати гигабайт из шестнадцати. Следующий прогон после этого
не падает честно, он ЗАДЫХАЕТСЯ: поездка идёт полчаса вместо трёх минут,
машина разгоняется до 132 км/ч, проверки «догнал» и «удар отнял скорость»
врут. Перед каждым прогоном:
`ps aux | grep headless_shell` и убить по номеру (`pkill -f chromium`
не ловит — процесс называется `headless_shell`). Признак задыхания —
`free -m`: занято больше двенадцати гигабайт при простое.

**Ловушка, в которую Клод попадался трижды:** нельзя править исходники, пока
идёт проверка через `vite` (`ride`, `play`, `shot`, `deploy`) — горячая
перезагрузка сносит состояние страницы, и проверка падает не по делу.
Сначала доделать правки, потом запускать. То же и с долгим `npm test`:
правка посреди прогона делает его результат смесью старого и нового.

**Где узкое место — смотреть ПРОФИЛИРОВЩИКОМ, а не мнением.**
`node --experimental-strip-types --cpu-prof --cpu-prof-dir=... tools/life.ts`
даёт файл, в котором видно, куда ушло время. 16 сентября он показал, что
52.6% всего времени города сидит в одной функции — и две догадки до этого
(`along`, длина прогона) оказались мимо.

---

## Известные дыры (найдены, НЕ починены)

0. **ПЯТЬДЕСЯТ ШЕСТЬ случайных построек из 250 (22%) дают уклон полотна выше
   предела.** Было записано «пять» — цифра врала, потому что врал прибор:
   порог стоял вчетверо выше предела (32% при пределе 8%) и прятал
   пятьдесят одну поломку. Порог починен 16 сентября, асфальт и тротуар
   разведены (решение 060).

   Из 84 случаев 60 — превышение до 15%, 24 — пятнадцать процентов и круче.
   Худшее: тротуар 24% на «серпантине» по холмам. По такому не пройти.

   На рукотворных сценах долг 8 из 36 сочетаний: `npm run scenes долг 8`.
   Чинится это высотным решателем в `world/` и полкой тротуара в `surface/` —
   обе папки заморожены словом Алекса. Долг назван числом: станет больше —
   проверка упадёт.
1. **Моста нет** — см. пункт 6 стола Алекса.
2. **Сцена «каша» проверку не проходит** и не проходила: ПЯТЬ бед из
   двадцати восьми. Здесь стояло «три» — цифра устарела; 17 сентября
   сверено запуском на коммите до правок и после: пять и там, и там,
   теми же числами. Пешеходы гуляют по проезжей части (модуль пешеходов
   не тронут), трое проехали на красный, габариты сходятся до 68% от касания.
   После переделки перекрёстков стало лучше: было 4 провала, стало 3,
   столкновения в узлах ушли совсем (42% → 110%), проехали 337 → 443 м.
   Корень — пункт 4 стола Алекса.
3. **Сквозь пешеходов можно проехать.** Сделано нарочно: сбивать людей никто
   не просил.
3a. **Пешеход на «каше» заходит на проезжую часть.** Это часть дыры 2 и то же,
   что в ней: модуль пешеходов решает, куда идти, по одной дороге, а на «каше»
   дорог в этом месте три.
4. **У машины игрока нет фонарей** — только у чужих.
5. **Превышение скорости город не считает.** Ограничения на улицах есть
   (знак 3.24, 60 по ПДД и 40 на тихих), но у игрока в `offence.ts` считаются
   только красный, встречная и непропуск пешехода.
6. **Нет:** ручной коробки, звука, неподрессоренной массы, домов, интерьеров.
7. **Трава без фактуры.** Одноцветная; вблизи видно.
8. ~~**Трафик падает на сгенерированном городе.**~~ **ЗАКРЫТА 15 сентября,
   но сначала выяснилось, что она никогда и не измерялась.** `npm run traffic
   город` полгода гонял «решётку»: сцены с именем «город» в списке не было,
   а `?? SCENES['решётка']` подставлял её МОЛЧА. Теперь неизвестное имя —
   ошибка со списком того, что есть. На настоящем городе нашлось и починено
   другое: машина не видела горящий красный, пока не выбрала маршрут поворота
   (а он берётся за 45 м до узла), и шла 78 км/ч на красный, начиная тормозить
   за 33 м при тормозном пути 39. Сейчас «город» проходит 28 из 28 и входит
   в `npm test`.
8a. **На «большом городе» 27 из 28.** 1.2 × 1.2 км. 17 сентября стало
   на один провал меньше: габариты больше не накладываются (решение 075
   убрал состояние «кузов между полосами»), остался один проезд на красный.
   Числа ниже — от 16 сентября, перемерить.

   **Застрявших больше нет: было 7, стало 0** (решение 065 — перестроение).
   Осталось:
   - «габариты нигде не наложились» — 68% от касания;
   - «превышают на 20–40, а не втрое» — 87 км/ч при знаке 40, то есть +47.

   Второе вскрылось только теперь, и оно настоящее: **машина не тормозит
   ПЕРЕД знаком, а въезжает в сорок на своих восьмидесяти и тормозит уже
   внутри.** На бумаге модель честна (`превышают типично` даёт ровно +40 —
   это потолок `desiredSpeed`), лишние семь километров — это разгон,
   принесённый с шестидесятки. Чинится тем же, чем крутой поворот впереди:
   знак впереди становится точкой с нужной скоростью в списке `holds`.
   Отдельного правила для этого не нужно, нужно применить существующее
   ещё к одной вещи.

   Замерено раньше, с какой плотности город запирал: при 120 машинах
   на те же 16.8 км застрявших было НОЛЬ. Теперь их ноль и при 329.

16. **БРАУЗЕРНАЯ ПОЛОВИНА ПРОВЕРКИ НЕ ЗАПУСКАЛАСЬ И БЫЛА КРАСНОЙ.**
    16 сентября `npm test` прогнали целиком — впервые за долгое время, — и
    выяснилось, что `npm run ride` падает. Причём падал он и ДО работ этого
    дня: на коммите `c3f3e8d` один провал («город назвал выезд на встречную»
    видел только проезд на красный).

    **Почему никто не знал.** В привычку вошло `npm test быстро`, в CI стояло
    `быстро`, а в самом CI было написано, что браузерная половина «гоняется
    руками перед выкладкой». Руками её не гоняли. Сайт выложили дважды.
    Проверка, которая не запускается, хуже сломанной: сломанная хотя бы
    кричит.

    **Что сделано в тот же день:** в CI добавлена вторая работа «браузер»
    (`npm test браузер` — новый третий способ запуска), и теперь браузерная
    половина гоняется на каждый push. Заржаветь ей больше негде.

    **Что найдено этой проверкой:** город переставал видеть машину игрока
    ровно в тот миг, когда подъезжал к ней вплотную (`gap > 0` вместо
    `gap > -LENGTH`) — та же беда, что была в `aheadOn` и там уже починена.
    Городская машина на сорока пяти въезжала игроку в зад, и удар ПРИБАВЛЯЛ
    ему скорости.

    **Починено, и дыра закрыта:** поездка 15 из 15 впервые за долгое время.
    В погоне игрок разгоняется до 56 км/ч вместо десяти, догоняет машину
    на 4.4 м, и удар отнимает скорость (56 → 37), а не прибавляет.

15. **Ответ города зависит от места машины в списке.** Кого обсчитали
    раньше, тот для остальных уже переехал: `o.shape`, `o.s`, `o.speed`
    читаются на живую, посреди шага. Предпроход (`targets`, `onRed`, `paths`,
    `entered`, `poses`) прикрывает часть этого снимком, но только часть,
    и получается смесь: где машина стоит — из снимка, на какой она дороге —
    на живую. Это прямо тот случай, который правило 0 называет «а если
    сначала вон тот».

    **Чем доказано, что это не теория.** 16 сентября полки по дорогам были
    сделаны сначала снимком — и вывод сдвинулся ровно на одну цифру из сорока
    (135% от касания стало 136%). Одна машина, переехавшая посреди шага.
    Больше ничего. Значит связь настоящая, а размер её — проценты.

    **Чем чинится:** один снимок положения всех машин на начало шага, и все
    вопросы «кто рядом» отвечаются по нему. Тогда полки станут тем же
    снимком, и разойтись им будет не с чем. Ответ при этом ИЗМЕНИТСЯ — это
    не оптимизация, а переделка, и мерить её надо всем набором заново.

19. **Утверждения вида «за 45 секунд случилось X» — лотерея.** В браузерной
    поездке половина проверок про это, и 17 сентября одна из них упала
    на том, что город стал ЛУЧШЕ: машина выезжала на встречную там, где её
    оставила предыдущая часть поездки, а после перестановки застройки там
    оказался светофор. Одну починили (решение 076): выезд на встречную
    проверяется детерминированно в `traffic`, браузеру оставлено «нарушение
    доехало до живой страницы». **Остальные такие же и ждут своей очереди:**
    «догнал чужую машину», «въехал в чужую машину», «осталась в пределах
    квартала». Каждая упадёт от следующей перестановки города.

17. **Тротуар нулевой ширины РВЁТ ПОВЕРХНОСТЬ.** В `world/road.ts` написано
    «0 — тротуара нет», но этим путём никто не ходил: `roadTypeForLanes`
    нуля не выдаёт никогда. 17 сентября внутриквартальному проезду поставили
    ноль — и на «городе» появилось под сотню точек, под которыми нет земли
    вообще; дом над одной из них повис, и это поймала проверка «дом и земля».
    Лента нулевой ширины вырождается, и раскрой её не сшивает.

    **Обойдено, а не починено:** у проезда стоит тротуар в метр. Чинить надо
    в `surface/`, а папка заморожена словом Алекса. Пока запрет в силе —
    **ноль в `sidewalk` ставить нельзя**, и знать об этом должен каждый,
    кто заводит новый тип дороги.

18. **ЗАКРЫТА 18 сентября.** Была: «машины жителей паркуются на улице,
    а не во дворе», и корнем назывался недостающий объезд по встречной.
    Корень оказался НЕ там и на уровень ниже: карманом объявлялась ПРАВАЯ
    ПОЛОСА ДВИЖЕНИЯ. Стоянки как места, отдельного от полосы, в данных
    не существовало вовсе — поэтому объезд и был обязателен. Полоса вида
    `parking` сняла и дыру, и нужду в объезде (решение 080).

    **Чему это учит, и это дороже самой починки.** Два вечера чинили
    механизм, которого не должно было быть. Признак был виден сразу и
    вслух: правило «стоящую надо объезжать» — это особый случай в правилах,
    а по правилу 8 первый же такой случай — повод спросить «почему это
    состояние вообще возможно?». Спросили только на третий раз.

19b. **В разрыве под въезд стоял дом, и это увидел только снимок.**
    18.09: все записи были на месте — въезд есть, проезд доходит до улицы,
    проверка «у каждого двора есть въезд» зелёная, — а с уровня глаз въезд
    во двор упирался в стену: детский сад в пятьдесят метров стоял поперёк
    проёма. Починено решением 083, заведена проверка «в разрыве под въезд
    не стоит дом» с подделкой `смежность-по-расстоянию`.

    **Чему учит.** Это третий раз за день, когда беда жила между слоями:
    запись о въезде честна, проезд честен, застройка честна по-своему —
    а вместе они дают стену. И третий раз, когда нашёл её НЕ инструмент,
    а снимок с уровня глаз. Правило 7 работает.

19a. **Двор мог быть островом, и никакая проверка этого не видела.**
    18 сентября: у всех тридцати шести дворов «города» был въезд, разрыв
    в застройке и нарисованный проезд — и НИ ОДИН не был соединён с улицей.
    Проезд начинался на красной линии, а дороги связываются осевыми линиями;
    до оси оставалось 10–20 метров. Машины во дворе стояли, но приехать
    и уехать было нельзя.

    **Починено** (решение 080), и — важнее — заведена проверка, которой
    не было: «до каждой дороги можно доехать» в `npm run plan`, со своей
    подделкой `двор-без-устья` (36 оторванных в городе, 16 в деревне).

    **Чему учит.** Проверка «у каждого двора есть въезд» смотрела на
    ЗАСТРОЙКУ и была честна. Связность — свойство СЕТИ, и спрашивать надо
    было у неё. Когда проверка и беда живут в разных слоях, зелёная галочка
    не значит ничего.

20. **ЗАКРЫТА 18 сентября, в тот же день.** Была: «у въезда во двор стоит
    светофор» — 18 лишних светофоров в «городе». Починено решением 082:
    светофор там, где есть магистральная И все подходы — улицы; улица
    от проезда отличается осевой разметкой. Светофоров 27 → 5, у выездов
    из дворов 18 → 0.

22. **Остальные ракурсы — это постоянные координаты, зашитые в код.**
    `вдоль` всегда смотрит из (−118, 52, −128) в (15, 2, 8), `перекрёсток`
    из (14, 46, −76) в (50, 0, −40). Город менялся, ракурсы смотрели в одно
    и то же место — и 18.09 это стоило прямого «где дворы, их нету блять»:
    построено было всё, а в меню не было ни одной кнопки, которая бы
    на это смотрела.

    Починен только `двор`: он ВЫЧИСЛЯЕТСЯ из проезда, и «ракурс, смотрящий
    в пустоту» для него невыразим. Остальные так и остались числами.
    Чинить их тем же способом: ракурс должен наводиться на то, что
    показывает, — на перекрёсток из сети, на дом из застройки.

    **Правило, которое из этого следует и которое дороже самой починки:
    шаг не закончен, пока в меню нет кнопки, показывающей сделанное.**
    Снимок, наведённый руками из терминала, доказывает, что вещь есть,
    и НЕ доказывает, что её найдёт человек.

25. **Проезд во дворе идёт ВДОЛЬ ДОМОВ (решение 089), но самого двора
    как места всё ещё нет.** Пара въездов выбирается на соседних сторонах,
    проезд огибает двор буквой «Г» с одним поворотом, стоянка оказалась
    у подъездов. Пешеходы доходят 389 из 400.

    **Две дороги туда, которые не сработали, — с числами, чтобы не пробовать
    снова:** излом буквой «С» (четыре поворота R=22) ломает четыре проверки
    сразу — походку на дуге тротуара, поворотники, слепую езду, и семеро
    встают намертво; сдвиг самого въезда к трети стороны ломает маршруты
    пешеходов (319 из 400 вместо 360, причина не найдена).

    **Чего нет:** середина квартала — по-прежнему пустой газон. Ни площадки,
    ни лавок, ни ограды, ни баков. Придомовая территория как СОДЕРЖИМОЕ
    двора не существует, и это следующая работа.

24. **Дыра 19 наполовину закрыта: браузерная поездка перестала быть лотереей.**
    21.09 три её утверждения провалились подряд на трёх прогонах, каждый раз
    разные, и ни одно — из-за поломки: «удар отнял у машины скорость»
    (132 → 135 км/ч, потому что газ держался нажатым и разгон обгонял удар),
    «догнал чужую машину» (за 45 с никого не встретил), «город заметил
    нарушение» (нарушать было неоткуда — машина после удара стояла поперёк).

    Первые два сняты: импульс и энергия удара проверяются детерминированно
    в `traffic`, а чтобы въехать в машину, надо её сперва догнать — это
    и есть проверка. Третье ПОЧИНЕНО по-настоящему: выезд на встречную
    теперь устраивается ДО удара, когда машина заведомо едет по своей полосе.

    **ДЫРА 19 ЗАКРЫТА 21.09 целиком.** Последним снято «въехал в чужую
    машину»: догнать за сорок пять секунд нельзя обещать — город видит
    игрока, тормозит и перестраивается, и догоняющий упирается в пустую
    полосу («догонял, но не задел» дважды подряд). Встречный курс вместо
    догона надёжен, но поездка от него перестала укладываться в четверть
    часа. Сам удар проверяется детерминированно в `traffic`, с подделкой.

    **Правило, которое из этого следует:** утверждение вида «за сорок пять
    секунд живой езды случилось X» — не проверка, а ставка. В браузере
    остаётся только то, что доказывает именно браузер: страница считает
    ТУ ЖЕ машину (разгон сходится с терминалом до сотых), нарушение доехало
    до живой страницы, и своя работа глаза не растёт вместе с городом.

23. **Две подделки перестали ломаться и сняты — а проверки остались
    без доказательства.** 21.09, после переделки квартала в периметральный,
    ни `life стоянка-в-полосе` (карманы на полосе движения), ни
    `выезд-замирает` (выезжающий замирает поперёк полосы) больше не валят
    `npm run life`: на новой геометрии двор не запирается. Держать
    подделку, которая не ломает, нельзя (правило 11), и они убраны.

    **Что осталось без подделки:** проверка `life` «кто не уехал — ждёт
    просвета, а не завис». Её утверждение по-прежнему меряется каждым
    прогоном, но синтетически сломать его сейчас нечем. Родственная
    проверка `traffic` «никто не встал намертво» тоже меряется, и она же
    поймала настоящий тупик 18.09 (кольцо из шестнадцати машин, двое
    по 76 с) — то есть ловить она умеет, доказано не подделкой, а делом.

21. **«Ничья» на Т-образном узле: двое встречных, идущих ПРЯМО, уступают
    друг другу.** Найдено 18.09 новой проверкой про дворы, но к дворам
    отношения не имеет: правила не развели двоих, и тогда уступает тот,
    кто подъехал позже, — а встречные, идущие прямо, разводиться и
    не должны, их пути не пересекаются.

    Замерено: 6.3 секунды за двухминутный прогон «города» на 128 машин.
    Число печатается строкой в отчёте `traffic` («уступали: двор … с,
    улица … с») — чтобы не росло молча. Корень искать в `nearest`/`RUB`:
    видимо, пути встречных сходятся ближе порога там, где дорога разрезана
    узлом.

9. **Пересборка «каши» — секунда** (на обычной сцене 0.2–0.35). Время
    линейно по числу треугольников, без всплеска. Значит большой город
    строится плитками — пересчитывается только задетая. Отдельный шаг, и
    делать его стоит не раньше, чем город станет большим.

---

### Вернулось из заброшенной ветки — разбор 12 сентября

Эти четыре замечания сделал чат, чья ветка не влилась никуда (город v1,
теперь `archive/city-v1`). В этой памяти их не было вообще. Найдены сверкой
13 сентября, решение 039.

10. ~~**Проверка пропускает ступеньку вчетверо круче предела.**~~
   **ЗАКРЫТА 16 сентября.** Порог стал `MAX_GRADE × 1.25`, асфальт и тротуар
   считаются раздельно, бордюр в счёт не идёт по построению. Настоящий долг
   вскрылся сразу же и записан в дыру 0: было «5 сломанных построек»,
   стало 56.
11. ~~**Планаризация квадратична.**~~ **ЗАКРЫТА — ПЕРЕМЕРЕНО 15 сентября,
    и старая цифра оказалась неправдой.** На мире 2 × 2 км планаризация стоит
    0.9 с из двадцати, то есть ни при чём; раскрой поверхности растёт почти
    линейно с числом треугольников (19 мкс на треугольник при 240 м, 23 мкс
    при 2 км). Настоящий потолок сидел в `plan.ts`: жадная расстановка служб
    пересчитывала покрытие ВНУТРИ сравнения при сортировке, то есть заново
    на каждую пару. Посёлок 2 км строился 2 мин 33 с, стал 4.4 с; посёлок
    1.2 км — 7.2 с, стал 0.44 с. Посёлок при этом получается ТОТ ЖЕ,
    отпечаток в отпечаток на 18 сочетаниях вида, размера и сида.
12. **Высоты полотна связаны глобально** (решение 015). Пока это так,
    «пересчитывается только задетая плитка» невыполнимо в принципе.
    Плитки, впрочем, больше не нужны для размера: мир 1.2 км собирается
    за 5 секунд целиком. Они понадобятся для ПЕРЕсборки на лету.

13a. ~~**Все участки стоят в углах перекрёстков.**~~ **НАЙДЕНО И ПОЧИНЕНО
    15 сентября.** Квартал отсчитывался шагами от КОНЦА улицы, а не от
    поперечных улиц; когда размер посёлка не кратен шагу квартала, раскладка
    съезжала на полквартала, и пустые углы оказывались посреди квартала.
    Замер: все 180 участков посёлка 600 м были ближе 46 м к поперечной улице,
    ближайший стоял ровно на её оси. Теперь рубежи квартала берутся у самих
    поперечных улиц. Ловится проверкой «перекрёстки не застроены», у которой
    заведомо сломанный вариант — ровно старое поведение (`npm run plan
    кварталы-от-конца`).

14. **Сид посёлка меняет дома, но не раскладку улиц.** Было: сид не менял
    вообще ничего, три разных сида давали побайтово один посёлок. Стало:
    от сида зависят этажность, ширина, цвет и крыша каждого дома, а через
    них — население (390 / 409 / 426 на одном и том же посёлке 220 м).
    Раскладка улиц и участков от сида по-прежнему не зависит, и это нарочно:
    её задают нормы, а не вкус.
13. **(s, t) не взято**, хотя `docs/how-roads-work.md` называет это «брать
    сейчас, пока дёшево»: пересчёт «вдоль-поперёк → x, z» повторён по коду
    многократно — правило 8 про третье повторение. **Сегодня не перепроверено.**

---

## Что дальше — план, по которому можно начать не думая

Порядок выбрал Клод, идёт сверху вниз и не спрашивает. Идея Алекса встаёт
туда, куда он скажет, или первой — по умолчанию первой.

У каждого шага заранее написано «готово = …» и бюджет: сколько файлов трогаем.
Это и есть защита от разгона — новый чат начинает работу с первой строки,
а не с изучения проекта.

**1. ~~Застройка, похожая на улицу.~~ СДЕЛАНО 15 сентября.** Дом — не коробка,
а объект: `src/city/дом.ts` даёт этажи, подъезды, окна, квартиры, жителей и
массу в тоннах. Показ рисует цоколь, стены, крышу, окна и двери шестью
пачками — весь город шесть вызовов отрисовки. Осталось на улице: заборы,
деревья и что-нибудь в 12-метровой полосе между тротуаром и красной линией —
сейчас там голый газон.

**2. ~~Плитки мира.~~ СДЕЛАНО ИНАЧЕ 15 сентября.** Оказалось, плитки не нужны:
размер мира стал свойством мира, а не постоянной, и потолок сидел в плане
посёлка, а не в геометрии. Есть три посёлка: «деревня» и «город» по 600 м
(3 780 жителей), «большой город» 1.2 км (15 120 жителей, собирается 5 с).

**3. ~~Трафик на сгенерированном городе.~~ СДЕЛАНО 15 сентября** — 28 из 28,
входит в `npm test`. Остаток — дыра 8a на «большом городе».

**4. ~~Порог проверки вчетверо выше предела.~~ СДЕЛАНО 16 сентября**
(решение 060). Порог стал `MAX_GRADE × 1.25`, асфальт и тротуар считаются
порознь. Настоящий долг вскрылся сразу: было «5 сломанных построек из 250»,
стало 56, и на рукотворных сценах 8 из 36.

**4a. ~~Уровни подробности для города.~~ ОТМЕНЁН 16 сентября — задача
исчезла** (решение 063). Считать грубо оказалось нечего: после полок по
дорогам ВЕСЬ «большой город» — 1223 машины, больше на его улицы физически
не помещается, — плюс 3200 пешеходов считается за 7.06 мс при бюджете
кадра 16.7. Было 13.14 мс на одних только 800 машинах с 800 пешеходами.
Осталось поднять потолок страницы (180 машин / 220 пешеходов) и снять
это глазами в браузере — это шаг 4b.

**4b. ~~Поднять потолок страницы и снять городом с уровня глаз.~~
СДЕЛАНО 16 сентября** (решения 064 и 065). Потолков больше нет: и страница,
и проверка спрашивают `сколькоМашин(net)` — 329 машин и 480 пешеходов
на «большом городе» вместо 180 и 220. По дороге нашлось и починено
настоящее: город был живым только своим первым углом (пешеходы на 29 улицах
из 84), а два правила перестроения запирали плотные улицы насмерть.

**4c. ~~Картинки на странице сравнения делаются РУКАМИ, и потому их нет.~~
СДЕЛАНО 16 сентября** (решение 066). Список того, что показывать, и список
того, что снимать, стали одним: таблица `КАРТИНКИ` в `page.mjs`. Было
«картинок 0» и успех, стало «картинок 14» и отказ собираться, если хоть
одного нет.

**4d. ~~Двенадцать метров голого газона между тротуаром и домами.~~
СДЕЛАНО 17 сентября** (решение 073). Деревья стоят вдоль всех улиц
и внутриквартальных проездов: 1340 на «городе», 5028 на «большом».
Цена: вызовов отрисовки 22 → 28, треугольников 410 384 → 549 744 —
то есть шесть вызовов на все деревья разом, а не по одному на дерево.
По дороге нашлось и починено: в `зелень.ts` было написано, что «дерево
на асфальте записать негде», а двадцать деревьев из 1360 стояли на
проезжей части — отступ мерился от СВОЕЙ улицы и не знал о поперечной.

Старая запись шага, для истории:
Замерено: у местной улицы полоса между тротуаром и красной линией четыре
метра, у магистральной девять с половиной, плюс три метра палисадника.
Сейчас там ничего. С уровня глаз это первое, что выдаёт нарисованность:
дома стоят в поле. Место дерева должно отсчитываться от края тротуара
наружу — тогда «дерево на асфальте» невыразимо.
> готово = снимок улицы с уровня глаз, на котором есть деревья, и кадр
> не просел; цифры — было / стало.
> бюджет: `src/city/зелень.ts` (уже написан и лежит в ветке, НО К ЭКРАНУ
> ЕЩЁ НЕ ПРИЛАЖЕН — это и есть весь остаток шага), `src/render.ts`,
> `src/main.ts`.

**4e. Машина не тормозит перед знаком.** См. дыру 8a: въезжает в сорок
на восьмидесяти. Механизм уже есть — точка впереди с нужной скоростью,
им же тормозят перед крутым поворотом.
> готово = на «большом городе» «превышают на 20–40» проходит.
> бюджет: `src/city/traffic.ts` — один файл.

**4f. РАЗНООБРАЗИЕ ЗАСТРОЙКИ — теперь это главное по форме.**
Замерено `npm run морфология`: у настоящих домов крупный больше мелкого
**в 12 раз** (40 и 484 м²), у нас — **в 1.13**. Под застройкой 11.3%
территории против настоящих 25.8%. Медиана следа при этом совпадает
(237 против 261) — то есть размер мы взяли верно, а разброс нет.
Корень один и он в `plan.ts`: `ФРОНТ_УЧАСТКА` — одна постоянная на весь
город, и глубина дома тоже одна. Дом занимает 45% своего участка,
настоящий — 66%.
> готово = разброс следов не меньше 4×, под домами не меньше 16%,
> и снимок улицы с уровня глаз, где дома разные.
> бюджет: `src/city/plan.ts`, `norms.ts`, `дом.ts`, `tools/морфология.ts`.

**4g. ОБЪЕЗД СТОЯЩЕЙ ПО ВСТРЕЧНОЙ. НАПИСАН, ЗАРАБОТАЛ, ОТКАЧЕН — читать это
перед тем, как браться снова.** 18 сентября сделан целиком и откачен на
последнем шаге. Работающий вариант лежит НЕ в ветке, а в переписке
(решение 079): его надо написать заново, зная всё, что ниже.

**Что уже проверено и работает.** Встречная полоса выражается номером −1
в `laneAcross` — одним и тем же числом, которым задаётся место машины
поперёк дороги, без второго числа рядом. Замер на «городе» с карманами
во дворе: **1317 объездов за две минуты, застрявших 0** (было 3, самый
долгий простой 67 с), парковка во дворе работает — 102 парковки, 84 выезда.

**Что сломалось и почему откачено.** Габариты наложились на 38% от касания
(было 114%) и на перекрёстке тоже 38%: объезжающие начали сталкиваться.
Это не долг, а сломанный инвариант. Нужна безопасность объезда, которой
нет: пока машина на встречной, никто не перепроверяет, что встречная всё
ещё свободна, и несколько машин одной очереди выезжают разом.

**Три ловушки, на которые ушёл вечер — не наступать снова:**

1. **Последняя дверь отбрасывала минус первую полосу молча.** В `wantLane`
   стоит одна проверка на все желания (`свободна(хочет)`), и она принимала
   только номера от нуля. Правило объезда возвращало −1, дверь его
   выбрасывала. Снаружи это выглядело как «правило есть, а объездов ноль»:
   разрешение выдавалось 20 688 раз за прогон и каждый раз пропадало.
   Дверь должна знать про встречную: у неё своя проверка — не «свободна ли
   полоса в мою сторону», а «хватит ли просвета выехать и вернуться».
2. **Хвост очереди видел соседа, а не причину.** Объезжать имел право
   только упёршийся НОСОМ в припаркованную. Замер: 116 338 раз машина
   стояла за стоящим соседом, который сам не припаркован. Нужна
   `причинаПробки` — ближайшая СТОЯЩАЯ на моей линии, а не ближайшая вообще.
3. **Встречный не держал дистанцию вообще** (`o.dir !== m.dir → continue`).
   На обычной дороге не вредило: встречная в трёх метрах, её отсекала
   проверка по смещению. С объездом кто-то оказывается НА ЧУЖОЙ ПОЛОСЕ,
   и «не вижу встречного» означает «еду в него». Особый случай убрать,
   скорость встречного в расчёт не брать: он не лидер, он препятствие.

**Две догадки, которые оказались мимо** (обе стоили по прогону):
условие «не объезжать у перекрёстка» — счётчик показал, что оно
не отказало НИ РАЗУ; длина требуемого просвета — тоже ни при чём.

> готово = 1317 объездов И габариты не ближе 100% от касания, застрявших 0.
> бюджет: `src/city/traffic.ts`, `src/city/lanes.ts`, `tools/traffic.ts`.
> Проверка «кто-то объехал стоящую по встречной» обязана быть: без неё
> правило — мёртвый код (замерено: при карманах только на магистралях
> объезд не срабатывает ни разу, случая нет).

**4h. Страна как параметр генерации** — просьба Алекса от 17.09.
Сегодняшний образец один и он чешский; норматив, по которому строится
город, — российский. Это уже два источника в одном городе. Страна должна
стать таблицей: размер квартала, красные линии, отступы, этажность,
норма хранения машин, есть ли палисадник, где стоят службы.
> готово = ползунок страны на странице меняет город, и `морфология`
> сверяет каждую страну со своим образцом.

**5. Тело пешехода:** ноги и руки видно, когда смотришь вниз. Сейчас человек
бесплотный — это первое, что ломает присутствие после того, как пошёл.
> готово = снимок вниз, на котором видно свои ноги при ходьбе.

**6. Звук** — шаги, дыхание, двигатель. По чужому опыту это половина
присутствия и самое дешёвое, что есть.
> готово = запись прогона, где слышно шаги по асфальту и по траве.

**7. Глубина резкости своим проходом.** Готовый из three рисует сцену вторым
заходом и выцвечивает кадр — снят. Расстояние наводки уже считается.
> готово = кадр, где резко то, на что смотришь, и цвет не поехал.

**8. Где кончается перекрёсток — одно место вместо трёх** (решение 042).
На нём стоят разметка, пешеходы и всё будущее вокруг узла.

**9. Интерьеры.** Цена замерена (решение 053): дело не в треугольниках,
а в вызовах отрисовки; дом склеивается в один кусок, пока в него не вошли.

**10. Небо и облака, мост и подпорная стенка** — дальше по списку.

---

## Назначение папок

- `src/world/` — мир: рельеф, дороги, сеть. Про экран не знает.
- `src/surface/` — поверхность из мира: области, раскрой, краска, призрак.
- `src/car/` — машина: паспорт, шина, опора под колесом, физика, управление.
- `src/person/` — человек на своих двоих: тело, шаг, шея, глаза, моргание.
- `src/растения/` — всё, что растёт и колышется: где трава растёт и где примята (`поле.ts`), как рисуется трава (`показ.ts`), дерево из веток (`дерево.ts`) и как рисуются деревья (`деревья.ts`).
- `src/city/` — городская жизнь: трафик, светофоры, пешеходы, нарушения.
- `src/types/` — описания типов для чужих библиотек, у которых их нет.
- `src/` — `main` склейка, `render` показ, `build` инструмент, `scenes` данные.
- `tools/` — всё, что запускается из терминала: проверки, снимки, разрезы.
- `shots/` — снимки, в репозитории не хранятся.
- `docs/` — справочники и журнал решений.

---

## Справочник

- `docs/decisions.md` — журнал решений, ограничивающих будущее. 32 штуки,
  новые снизу. Читать при сомнении «почему так, а не иначе».
- `docs/how-cars-work.md` — как считают машину: шина, подвеска, трансмиссия,
  аэродинамика, уровни подробности для города. Каждая строка тех.паспорта →
  формула. Числа Viper посчитаны и сверены с замерами. Чужой опыт, не решения.
- `docs/how-cities-work.md` — как устроен город: радиусы обслуживания, нормы
  на тысячу жителей, красные линии, бордюр, где на самом деле стоят магазины.
  Российские нормативы со ссылками и честным статусом у каждого числа.
- `docs/how-presence-works.md` — как делается присутствие: глаза и голова,
  шея, шаг, моргание, и чем маскируется дешёвая картинка. Измерения человека
  и чужой опыт, не наши решения.
- `docs/how-roads-work.md` — как устроены дороги у тех, кто это прошёл:
  OpenDRIVE, SUMO, Cities: Skylines, Transport Fever, CityEngine, плюс нормы
  настоящего дорожного проектирования. Чужой опыт, не наши решения.

**Ссылки:**
- живой мир — https://depth4.github.io/lifeboonremake/
- сравнение вариантов дороги — https://depth4.github.io/lifeboonremake/report.html
- он же артефактом, если сайт недоступен —
  https://claude.ai/code/artifact/e7d29406-9459-4e0a-95b1-d690d841481b
- страница сравнения артефактом —
  https://claude.ai/code/artifact/85404842-3f65-4c10-8222-df1f99f69f42
- живой расчёт паспорта машины (крутить ползунки и смотреть, что выйдет) —
  https://claude.ai/code/artifact/53089019-7f88-4fd1-908d-a2a4d92e3492

---

## Куда идём (видение Алекса)

Симулятор жизни, склеенный с открытым миром в духе GTA/SAMP. Одновременно:

- **Текстовый симулятор жизни** — жизнь персонажа, работа, деньги, отношения.
- **Открытый мир** — за руль может сесть человек, которому симулятор жизни
  не интересен: покататься, поработать таксистом, погонять.
- **Персонаж** — ходьба, бег, прыжки, взаимодействие с миром.
- **Интерьеры** — в здания можно входить. Из окна виден настоящий город,
  который живёт своей жизнью. Ключевое для ощущения присутствия: окно —
  доказательство, что мир существует без игрока.
- **Мультиплеер** в будущем: серверы, общий мир. Отсюда требование, чтобы мир
  считался без экрана.
- Возможно животные.
- Правдоподобный город — в перспективе на основе реальных карт (решение 003).

**Текстовый симулятор и открытый мир — не два проекта и не два режима.**
Это два окна в один и тот же мир и две степени приближения к нему.

**Первый играбельный кусок:** проехать на машине по дороге в маленьком
рукотворном квартале. — сделано.

### Список Алекса от 13 сентября — целиком, по цене

**Глаза и голова — дёшево, это математика камеры, а не картинка.** Зрачки
и голова поворачиваются отдельно; голова с задержкой и лёгким смазом; на
360° не повернуться — нужен доворот телом, и он должен чувствоваться; при
ходьбе голова трясётся; моргание; чуть шире поле зрения с мягкими краями.
Это то, из чего делается присутствие, и стоит оно почти ничего.

**Картинка — средне, это проходы после кадра (post-processing).** Фокус на
предмете с размытым фоном; при взгляде на движущееся смазано всё; свечение
вокруг ярких; остаточные блики после яркого. Съедают заполнение экрана,
на слабом железе режутся первыми — поэтому каждое включается отдельно.

**Дорого.** Живые объёмные облака (считаются лучом сквозь объём, самая
тяжёлая вещь в списке). Реалистичное небо — средне, если без облаков.
Интерьеры: подъезд, квартира, вид из окна на живой город.

**Персонаж.** Тело, ноги, руки; ходьба. Пешеходный режим — вылезти из машины
и пойти.

**Жители — решения 044 и 045.** У каждого свой номер, профессия, квартира,
машина, распорядок. За любым можно следить, и он не исчезнет.
Кто он — вычисляется из номера и сида города, забыть это невозможно.
Что с ним случилось — **хранится, и не выборочно, а по всему городу**:
замерено, миллион жителей это 23 МБ и 123 мс на сохранение. «Житель,
который не сохранился» невыразим. Исключение одно и задано числом:
несколько процентов приезжих, которые уходят насовсем. Замер — `npm run load`.

**Мир по сиду на реальный город.** OSM даёт улицы и дома; всё, чего в OSM
нет, — баннеры, растительность, мелочь — додумывается из сида, одинаково
у всех и каждый раз.

**Машина живёт.** Выхлоп, износ. Ямы на дорогах — из сида, меняются редко,
но меняются.

**Песочница поверх одного мира.** Выживание, зомби, текстовый симулятор —
не три проекта и не три режима, а разные правила поверх одних и тех же
жителей. Устройство жителя из решения 044 это позволяет: житель — функция
плюс отклонения, и «зомби» это просто другое отклонение.

---

### Список Алекса от 17 сентября — целиком, по цене

Многое здесь уже есть в списке от 13 сентября выше (глаза и голова, фокус
и смаз, свечение и блики, тело персонажа, небо и облака, жители с номером
и машиной, ямы из сида). Ниже — то, что добавилось, и то, что Алекс назвал
заново: повтор — не ошибка, а вес.

**Анимация — главное новое требование, и оно про устройство, а не про
картинку.** «Руки и ноги не дёргаются, а проходят полный путь. Каждое
действие сопровождается анимацией, ничего не происходит просто так»:
сесть, встать, лечь, побежать, прыгнуть, открыть ручку машины, переключить
передачу, потрогать мультимедиа.
> Это то же правило 0, только про движение: **состояние «персонаж
> телепортировался в машину» должно быть невыразимо**. Не «добавить
> анимацию посадки», а сделать так, чтобы попасть в машину можно было
> ТОЛЬКО пройдя путь руки к ручке. Тогда анимация — не украшение,
> а единственная дорога между двумя состояниями.
> Цена: это переделка того, как персонаж вообще меняет состояние. Делать
> её надо ДО того, как состояний станет много.

**Ветер, и всё, что от него зависит.** Растительность колышется по
направлению и силе ветра; бельё на балконах; шторы в окнах.
> Ветер — одно число на весь мир (направление и сила), и всё колышется
> ОТ НЕГО, а не каждое само по себе. Тогда «трава качается в одну сторону,
> а бельё в другую» невыразимо. Дёшево, если сделать так сразу.

**Дом — не коробка с окнами.** Шторы, приоткрытые окна, кондиционеры,
бельё на балконах. Сейчас окно — плоский светящийся прямоугольник.
> Всё это — свойства КВАРТИРЫ, а квартира уже есть в `дом.ts`. Значит
> выводится из номера квартиры, как и всё остальное, и не хранится.

**Улица.** ЛЭП-столбы и провода. Мусорки. Деревья (шаг 4d, файл написан).
> Столб и провод — то же, что дерево: свойство улицы, место отсчитывается
> от края тротуара. Провод между столбами — провисающая кривая, считается
> из двух концов.

**Велосипед.** Юлит, надо держать баланс, при сильном крене в повороте
можно упасть.
> Это отдельная физика, не упрощённая машина. Дорого, но честно: у нас
> уже есть машина с переносом веса, велосипед — та же природа.

**Полиция и погони.** Реалистичные погони.
> У нас уже есть `offence.ts`: город ЗАМЕЧАЕТ нарушения игрока. Погоня —
> следующий шаг из того же: нарушение видит не абстракция, а конкретный
> житель в форме, у которого есть номер, машина и маршрут.

**Пешеходный мод** — вылезти из машины и пойти. Частично есть.

**«Каждый житель в городе не болванка. У каждого свой номер и авто.»**
Это уже сделано (решения 044, 045, 064): 15 120 жителей большого города,
у каждого дом, дело, распорядок и машина, и на улицах их теперь видно
по всему городу, а не в одном углу.

**Ямы на дорогах — процедурные, динамически меняются, но на момент выезда
из города сохраняются.**
> Ключ в последних словах. «Меняется, но запоминается» — это ровно
> устройство жителя: функция от сида плюс отклонения, и хранить надо
> только отклонения. Яма, которую никто не трогал, вычисляется; яма,
> которую залатали или углубили, — отклонение.

**Полная иммерсивность** — это не пункт, а мера для всех остальных.
